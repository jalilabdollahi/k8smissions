{
  "module": "module-11-performance",
  "levels": {
    "level-1-vpa-off": {
      "description": "A VerticalPodAutoscaler exists for the deployment, but pods keep getting OOMKilled. The VPA is collecting usage data and generating recommendations — but it's configured in `Off` mode, meaning it never actually applies those recommendations. Pods run with their original resource requests forever.",
      "hint1": "Check what the VPA is recommending: `kubectl describe vpa my-app-vpa -n k8smissions`. Look at the recommendation section and compare it with the pod's current resource requests.",
      "hint2": "The VPA shows recommendations like 'request 500m CPU and 512Mi memory' but the pod is still running with the original values. Look at the VPA's `updatePolicy` section — it controls whether recommendations are applied.",
      "hint3": "The VPA has `updatePolicy.updateMode: Off`. This means VPA only observes and reports — it never acts. Change it to `updateMode: Auto` so VPA will evict and restart pods with right-sized resource requests.",
      "reveal": "## What went wrong\n\nVPA `updateMode: Off` is a read-only mode — the VPA admission controller does not mutate pod requests. This is useful for auditing what VPA *would* recommend without actually changing anything. With `Off`, OOMKills continue even when the VPA clearly shows the pod needs more memory, because no mechanism applies the recommendation.\n\n## Fix\n\n```yaml\nupdatePolicy:\n  updateMode: Auto\n```\n\n## Why this matters\n\nVPA has four modes:\n- `Off`: recommendations only, never applied\n- `Initial`: applied only at pod creation, never updated\n- `Recreate`: applied at creation and by evicting live pods\n- `Auto`: same as Recreate today (may support in-place updates in future)\n\n`Auto` is the production-ready mode — VPA evicts pods that are significantly over- or under-resourced and lets the Deployment controller replace them with correctly-sized ones. Pair VPA with a PodDisruptionBudget to control eviction rate and prevent all pods from being evicted simultaneously."
    },
    "level-2-hpa-no-metrics": {
      "description": "An HPA is scaling the deployment up under load — but after load drops, it scales back down too fast and too aggressively, causing a saw-tooth pattern of over-scaling and under-scaling. The HPA has no scale-down behavior configured, so it uses the Kubernetes default: remove as many replicas as possible as fast as possible.",
      "hint1": "Watch the HPA in real time: `kubectl get hpa my-hpa -n k8smissions -w`. You should see rapid replica count changes after load spikes end — up to 10 replicas during load, dropping immediately to 2 when load drops.",
      "hint2": "Check if the HPA has a `behavior` section: `kubectl get hpa my-hpa -n k8smissions -o yaml | grep -A20 behavior`. Without it, Kubernetes uses its default aggressive scale-down policy.",
      "hint3": "The HPA is missing a `behavior.scaleDown` configuration. Add `stabilizationWindowSeconds: 300` to wait 5 minutes before scaling down, and a policy capping scale-down at 10% of replicas per minute.",
      "reveal": "## What went wrong\n\nWithout `behavior.scaleDown`, the HPA uses the default policy: scale down as quickly as needed with no stabilization window. This causes thrashing — the HPA removes all extra replicas the moment metrics drop, only to scale them back up when load returns seconds later. New pod startup latency during each scale-up causes user-visible errors.\n\n## Fix\n\n```yaml\nbehavior:\n  scaleDown:\n    stabilizationWindowSeconds: 300\n    policies:\n    - type: Percent\n      value: 10\n      periodSeconds: 60\n```\n\n## Why this matters\n\nThe `stabilizationWindowSeconds` tells the HPA to look at the maximum desired replica count over the past N seconds and use that as the scale-down target — avoiding immediate drops on transient metric dips. The `policies` list adds rate limiting: in this case, maximum 10% of replicas removed per 60-second window. Scale-up should remain aggressive (fast response to load) while scale-down should be conservative (no saw-tooth). You can configure `behavior.scaleUp` separately to control burst behavior."
    },
    "level-3-dns-ndots": {
      "description": "A high-throughput application makes hundreds of DNS lookups per second using short names like `db`, `cache`, `api`. These lookups are generating massive load on kube-dns and have high latency. The default `ndots: 5` setting causes each short name to be searched across 5 DNS suffixes before resolving, multiplying DNS traffic by 5x.",
      "hint1": "Count DNS queries per second on a pod: `kubectl exec dns-heavy-app -n k8smissions -- cat /etc/resolv.conf`. Look at the `ndots` and `search` values — each short lookup triggers a search through each of those domains.",
      "hint2": "With `ndots: 5` (the default), a lookup for `db` becomes 5 queries: `db.k8smissions.svc.cluster.local`, `db.svc.cluster.local`, `db.cluster.local`, `db.ec2.internal`, `db`. Only the last one or the FQDN succeeds — meaning 4 failed lookups per successful one.",
      "hint3": "Add `dnsConfig.options` with `ndots: 2` to reduce the search suffix attempts. Also change the DNS lookup in the container command to use the full FQDN `db.k8smissions.svc.cluster.local` instead of `db`.",
      "reveal": "## What went wrong\n\n`ndots: 5` means: if the hostname has fewer than 5 dots, try each search domain before attempting the name as-is. A lookup for `db` has 0 dots — so Kubernetes tries `db.k8smissions.svc.cluster.local`, `db.svc.cluster.local`, `db.cluster.local`, `db.default.svc.cluster.local` (etc.) before resolving. 100 lookups/second becomes 500+ DNS queries/second, overwhelming kube-dns.\n\n## Fix\n\n```yaml\nspec:\n  dnsConfig:\n    options:\n    - name: ndots\n      value: '2'\n  containers:\n  - name: app\n    command:\n    - /bin/sh\n    - -c\n    - while true; do nslookup db.k8smissions.svc.cluster.local; sleep 0.1; done\n```\n\n## Why this matters\n\nTwo strategies reduce DNS overhead:\n1. **Lower ndots**: `ndots: 2` means names with 2+ dots are treated as FQDNs and bypass the search list entirely. `db.k8smissions` has 1 dot — still searches. `db.k8smissions.svc` has 2 dots — tries as-is first.\n2. **Use FQDNs**: always append `.svc.cluster.local` in service URLs to bypass all search path lookups.\n\nFor extreme DNS traffic, enable NodeLocal DNSCache (level 10) which caches responses at the node level, eliminating round-trips to kube-dns for repeated lookups."
    },
    "level-4-cpu-throttling": {
      "description": "An API server deployment has excellent latency at low load but degrades dramatically under traffic spikes. CPU throttling is the culprit: the container's `cpu.limits` is set equal to `cpu.requests` (both 100m), which means the container is immediately throttled the moment it tries to use any CPU burst above its guaranteed share.",
      "hint1": "Check CPU throttle metrics: `kubectl top pod -n k8smissions -l app=api-server`. Then look at the resource configuration: `kubectl get deployment api-server -n k8smissions -o yaml | grep -A8 resources`",
      "hint2": "You should see `requests.cpu: 100m` and `limits.cpu: 100m`. When a traffic spike hits, the nginx process tries to use more than 100 millicores — but the Linux CFS throttle blocks it. The throttle continues for the rest of the 100ms scheduling window, causing latency spikes.",
      "hint3": "The CPU limit equals the CPU request — there's no headroom for bursting. Increase `limits.cpu` to `1000m` (10x the request) to allow the container to use available CPU capacity during spikes without being throttled.",
      "reveal": "## What went wrong\n\nWhen `limits.cpu == requests.cpu`, the container gets exactly its reserved CPU share and nothing more. Under load, the Linux CFS (Completely Fair Scheduler) enforces the quota: once the container uses 100ms of CPU in a 100ms window, it's throttled for the rest of the window. This creates bursty latency that looks like slow responses every 100ms.\n\n## Fix\n\n```yaml\nresources:\n  requests:\n    cpu: 100m\n    memory: 256Mi\n  limits:\n    cpu: 1000m\n    memory: 512Mi\n```\n\n## Why this matters\n\nCPU requests determine scheduling placement and guaranteed share. CPU limits determine the throttle ceiling. Setting them equal creates a Guaranteed QoS class (good for predictable latency) but removes all burst capacity. Setting limits higher (or omitting limits entirely) allows the container to use spare CPU on the node without throttling. The right ratio depends on your workload: latency-sensitive services often want limits 3–10x the request; batch jobs often want limit = request for predictable throughput. Prometheus metric `container_cpu_cfs_throttled_seconds_total` reveals throttling in production."
    },
    "level-5-connection-pool": {
      "description": "A web application with 10 replicas is causing 'too many connections' errors on the database. Each pod has `DB_POOL_SIZE: 1`, so each pod maintains exactly one database connection — but with only 1 connection per pod and no overflow capacity, concurrent requests queue up and time out during traffic spikes.",
      "hint1": "Check the app's database connection environment variables: `kubectl get deployment web-app -n k8smissions -o yaml | grep -A10 env`. Count the total possible connections: replicas × pool size.",
      "hint2": "With 10 replicas and `DB_POOL_SIZE: 1`, the app has a maximum of 10 simultaneous database queries across the entire deployment. Any traffic spike beyond 10 concurrent DB operations causes requests to queue. No `DB_POOL_TIMEOUT` means queued requests wait indefinitely.",
      "hint3": "Increase `DB_POOL_SIZE` from `1` to `5` to allow 5 concurrent connections per pod (50 total). Also add `DB_POOL_TIMEOUT: 30` and `DB_POOL_MAX_OVERFLOW: 10` to handle burst traffic gracefully.",
      "reveal": "## What went wrong\n\n`DB_POOL_SIZE: 1` per pod means the entire deployment (10 pods) has a maximum of 10 simultaneous database connections. A connection pool of 1 creates a serialization bottleneck — every request that needs the database must wait for the single connection to be free.\n\n## Fix\n\n```yaml\nenv:\n- name: DB_POOL_SIZE\n  value: '5'\n- name: DB_POOL_TIMEOUT\n  value: '30'\n- name: DB_POOL_MAX_OVERFLOW\n  value: '10'\n```\n\nTotal max connections: 10 pods × (5 pool + 10 overflow) = 150 max connections.\n\n## Why this matters\n\nConnection pool sizing is a balance: too small → request queuing and timeouts. Too large → database connection exhaustion (PostgreSQL default max is 100; RDS allows ~3× memory/12.5MB). Rule of thumb: `pool_size × replicas ≤ database max_connections × 0.8`. The `max_overflow` parameter allows temporary burst above `pool_size`, automatically cleaned up when the burst subsides. `pool_timeout` prevents requests from waiting forever — fail fast with a meaningful error is better than cascading timeouts. For very high replica counts, consider PgBouncer as a connection pooler in front of the database."
    },
    "level-6-readiness-gate": {
      "description": "After deploying a new version of a Java application, users experience 502 errors for 30–60 seconds. The rolling update is replacing pods, and new pods are immediately marked Ready and receive traffic — but the JVM is still warming up and the application hasn't finished loading its caches yet. Requests hit a pod that isn't truly ready.",
      "hint1": "Watch pod status during a rollout: `kubectl rollout status deployment/java-app -n k8smissions`. Notice pods transition to Ready very quickly, but check if they actually respond: `kubectl exec <pod> -- curl -f http://localhost:8080/health/ready`",
      "hint2": "The pod reaches Running state and Kubernetes marks it Ready because there's no readinessProbe defined. Without a probe, Kubernetes assumes the pod is ready the moment the container starts — but a Java application can take 30+ seconds to fully initialize.",
      "hint3": "The Deployment has no `readinessProbe`. Add one that checks `GET /health/ready` on port 8080 with `initialDelaySeconds: 30` to give the JVM time to warm up before traffic is sent.",
      "reveal": "## What went wrong\n\nWithout a `readinessProbe`, kubelet marks the container as ready immediately when it starts running. The rolling update controller sees the new pod as ready and proceeds to terminate the old pod — before the Java application has loaded its Spring context, opened database connections, and warmed its caches. The brief window where both old and new pods are in transition creates the 502 errors.\n\n## Fix\n\n```yaml\nreadinessProbe:\n  httpGet:\n    path: /health/ready\n    port: 8080\n  initialDelaySeconds: 30\n  periodSeconds: 5\n  failureThreshold: 3\n```\n\n## Why this matters\n\nReadiness probes gate traffic routing — a pod not passing its readiness probe is removed from Service endpoint lists and receives no traffic. This is distinct from liveness probes (which restart failing containers). The `initialDelaySeconds` skips probing during known-slow startup. The `periodSeconds` and `failureThreshold` control how quickly readiness is detected and lost. For JVM applications, `initialDelaySeconds: 30–60` is typical. For containerized apps exposing a `/health/ready` endpoint (Spring Actuator, Quarkus health, Go health checks), the probe should verify actual readiness — not just 'process is running'."
    },
    "level-7-pdb-too-strict": {
      "description": "Node maintenance is blocked — `kubectl drain` fails with 'Cannot evict pod as it would violate the pod's disruption budget'. The PodDisruptionBudget requires a minimum of 3 pods available, but the deployment only has 3 replicas total. This means zero pods can ever be evicted, making node maintenance impossible.",
      "hint1": "Try draining a node and read the error: `kubectl drain <node> --ignore-daemonsets`. Check the PDB: `kubectl get pdb my-pdb -n k8smissions -o yaml`",
      "hint2": "The PDB shows `minAvailable: 3` and the deployment has `replicas: 3`. With 3 pods required minimum and 3 total, evicting any single pod would drop below 3 — so the PDB blocks all evictions. `kubectl describe pdb my-pdb -n k8smissions` should show `ALLOWED DISRUPTIONS: 0`.",
      "hint3": "The PDB has `minAvailable: 3` but the deployment only has 3 replicas — no disruptions are ever allowed. Change `minAvailable` to `2` to allow 1 pod to be evicted at a time while keeping 2 available.",
      "reveal": "## What went wrong\n\n`minAvailable: 3` with `replicas: 3` means the PDB allows zero disruptions: `allowed_disruptions = replicas - minAvailable = 3 - 3 = 0`. Every eviction attempt (node drain, cluster autoscaler scale-down, rolling update) is blocked. Nodes can never be drained for maintenance.\n\n## Fix\n\n```yaml\nspec:\n  minAvailable: 2\n```\n\nThis allows `3 - 2 = 1` disruption at a time — one pod can be evicted while 2 remain available.\n\n## Why this matters\n\nPDBs must be sized relative to the actual replica count. Common mistake: setting `minAvailable` equal to `replicas`, which makes the PDB completely block disruptions. Better: use `maxUnavailable: 1` (absolute) or `maxUnavailable: 33%` (percentage) which automatically scales with replica count changes. A PDB that blocks all disruptions is worse than no PDB — it prevents maintenance without providing any actual benefit. Always verify `kubectl describe pdb` shows `ALLOWED DISRUPTIONS > 0` after creating a PDB."
    },
    "level-8-service-mesh-timeout": {
      "description": "Users are getting 504 Gateway Timeout errors for a specific backend service. The Istio VirtualService has `timeout: 15s` — but this backend performs batch data processing that legitimately takes 30–120 seconds. The mesh timeout is cutting connections that should succeed.",
      "hint1": "Check the Istio VirtualService timeout: `kubectl get virtualservice backend-vs -n k8smissions -o yaml | grep timeout`",
      "hint2": "The VirtualService has `timeout: 15s`. Check the backend logs to see if requests are completing: `kubectl logs -n k8smissions -l app=backend --tail=50`. You should see successful responses after 30–60 seconds — but the VirtualService is canceling them at 15 seconds.",
      "hint3": "The `timeout: 15s` is too short for operations that take up to 120 seconds. Increase it to `timeout: 120s` and add a `retries` block with `attempts: 3`, `perTryTimeout: 30s`, and `retryOn: 5xx,reset,connect-failure` to handle transient failures.",
      "reveal": "## What went wrong\n\nThe VirtualService timeout of 15 seconds is shorter than the backend's actual processing time. When a request exceeds the timeout, Istio's Envoy sidecar cancels the request and returns a 504 to the caller — even if the backend would have successfully completed the work a few seconds later.\n\n## Fix\n\n```yaml\nhttp:\n- timeout: 120s\n  retries:\n    attempts: 3\n    perTryTimeout: 30s\n    retryOn: 5xx,reset,connect-failure,retriable-4xx\n  route:\n  - destination:\n      host: backend.k8smissions.svc.cluster.local\n      port:\n        number: 8080\n```\n\n## Why this matters\n\nTimeout and retry configuration in a service mesh are separate concerns:\n- **timeout**: the total budget for the entire request including all retries\n- **perTryTimeout**: the budget for a single attempt before retrying\n- **retryOn**: which error conditions trigger a retry\n\nThe key invariant: `perTryTimeout × attempts ≤ timeout` (with some slack). Retrying non-idempotent operations (POST, payments, writes) requires application-level idempotency keys — don't blindly retry all 5xx errors if the backend doesn't handle duplicate requests safely."
    },
    "level-9-etcd-quota": {
      "description": "The API server is returning 'etcdserver: mvcc: database space exceeded' errors and all write operations are failing. etcd's storage backend has hit its quota limit of 2GB. The cluster has been running long enough that accumulated key/value data (ConfigMaps, Secrets, events, old object revisions) has filled the quota.",
      "hint1": "Check the etcd quota configuration: `kubectl get configmap etcd-config -n kube-system -o yaml | grep quota-backend-bytes`",
      "hint2": "The value `2147483648` is 2GB. etcd is refusing writes because it has hit this hard limit. You can verify by checking etcd health: `kubectl exec -n kube-system etcd-<node> -- etcdctl endpoint status --write-out=table`",
      "hint3": "The `quota-backend-bytes` is set to 2GB (`2147483648`). Increase it to 8GB (`8589934592`) — the recommended production value. Also verify `auto-compaction-retention: 1h` is set to prevent accumulation of old revisions.",
      "reveal": "## What went wrong\n\nThe etcd quota limit of 2GB is etcd's default and is too small for production clusters. A busy cluster accumulates revisions of every object (ConfigMaps, Secrets, custom resources) until compaction runs. When the quota is exceeded, etcd raises an alarm and refuses all write operations — the cluster becomes read-only.\n\n## Fix\n\n```yaml\ndata:\n  quota-backend-bytes: '8589934592'\n  auto-compaction-mode: periodic\n  auto-compaction-retention: 1h\n```\n\nTo defragment etcd after increasing the quota:\n```bash\netcdctl defrag --cluster\netcdctl alarm disarm\n```\n\n## Why this matters\n\nThe Kubernetes documentation recommends 8GB as the maximum safe etcd database size for production clusters. Beyond 8GB, etcd performance degrades significantly. Key mitigation strategies:\n1. **Compaction**: `auto-compaction-retention: 1h` removes old revisions older than 1 hour, keeping the database small\n2. **Defragmentation**: compaction marks space as free but doesn't shrink the file; `etcdctl defrag` reclaims it\n3. **Event TTL**: configure `--event-ttl=1h` on the API server to expire old Events\n4. **Monitoring**: alert when etcd database size exceeds 75% of quota"
    },
    "level-10-node-local-dns": {
      "description": "DNS resolution latency is high cluster-wide — even simple service lookups take 5–10ms. NodeLocal DNSCache is installed but not active. All DNS queries still travel from pods through the pod network to kube-dns, adding unnecessary network hops. The feature is installed but disabled by a flag.",
      "hint1": "Check NodeLocal DNSCache status: `kubectl get configmap node-local-dns -n kube-system -o yaml | grep enabled`",
      "hint2": "The ConfigMap shows `enabled: 'false'`. NodeLocal DNSCache runs as a DaemonSet on each node, listening on a link-local IP (169.254.20.10). When disabled, pods still use the regular kube-dns path, which requires network traversal to the kube-dns pod.",
      "hint3": "The `enabled` field in the `node-local-dns` ConfigMap is set to `false`. Change it to `true` to activate NodeLocal DNSCache, which will serve DNS responses from a cache on the local node without traversing the pod network.",
      "reveal": "## What went wrong\n\nNodeLocal DNSCache is installed (the DaemonSet and ConfigMap exist) but the `enabled: 'false'` flag keeps it inactive. Without it, every DNS query travels over the pod network to a kube-dns pod — adding a network round-trip and making DNS a shared bottleneck across all pods.\n\n## Fix\n\n```yaml\ndata:\n  enabled: 'true'\n```\n\n## Why this matters\n\nNodeLocal DNSCache (node-local-dns) runs CoreDNS as a DaemonSet on every node, listening on a link-local IP (169.254.20.10). Pods that use this IP for DNS bypass the pod network entirely — responses come from a local process on the same node. Benefits:\n1. **Latency**: sub-millisecond for cached responses vs. 1–10ms for network round-trips\n2. **Scale**: eliminates kube-dns as a centralized bottleneck; each node serves its own pods\n3. **Reliability**: DNS continues working if kube-dns pods are briefly unavailable (cache serves stale responses)\n4. **Connection tracking**: avoids conntrack race conditions that cause DNS packet drops under high query rates\n\nNode-local DNS caches become especially critical when combined with low ndots values (level 3)."
    },
    "level-11-graceful-shutdown": {
      "description": "Rolling updates cause request errors — clients receive connection resets and incomplete responses during pod termination. The pod has a `terminationGracePeriodSeconds: 5`, which is too short for in-flight requests to complete. Additionally, there's no preStop hook to delay traffic removal from the load balancer before the container starts shutting down.",
      "hint1": "Time how long in-flight requests take: `kubectl exec <pod> -n k8smissions -- curl -s -w '%{time_total}' http://localhost:80/slow-endpoint`. Then check the termination config: `kubectl get deployment web-server -n k8smissions -o yaml | grep -A5 terminationGrace`",
      "hint2": "The `terminationGracePeriodSeconds: 5` gives pods only 5 seconds to finish requests before SIGKILL. Long-running requests (>5s) are killed mid-flight. There's also a race: when a pod gets SIGTERM, the load balancer may still route traffic to it for several seconds while endpoint updates propagate.",
      "hint3": "Increase `terminationGracePeriodSeconds` to `60`. Add a `lifecycle.preStop.exec` hook that runs `sleep 10` — this delays SIGTERM for 10 seconds, giving the load balancer time to stop routing traffic to the pod before the app starts shutting down.",
      "reveal": "## What went wrong\n\nTwo separate races cause request errors during shutdown:\n1. **Too-short grace period**: `terminationGracePeriodSeconds: 5` kills in-flight requests that take longer than 5 seconds\n2. **Load balancer lag**: when a pod receives SIGTERM, the kube-proxy/iptables update removing it from Service endpoints takes 1–3 seconds. New requests still arrive during this window.\n\n## Fix\n\n```yaml\nterminationGracePeriodSeconds: 60\ncontainers:\n- name: web\n  lifecycle:\n    preStop:\n      exec:\n        command: [\"/bin/sh\", \"-c\", \"sleep 10\"]\n```\n\n## Why this matters\n\nThe graceful shutdown sequence:\n1. Pod receives SIGTERM → preStop hook runs (`sleep 10`)\n2. During those 10 seconds, endpoint update propagates to kube-proxy → load balancer stops routing new requests to this pod\n3. After preStop completes, SIGTERM is sent to the container process → app finishes in-flight requests\n4. After `terminationGracePeriodSeconds`, SIGKILL is sent\n\nThe `sleep 10` in preStop is the standard pattern for the 'endpoint propagation window'. The grace period must be longer than `preStop duration + max request duration`. For HTTP/1.1 servers, set `connection: close` headers to drain connections."
    },
    "level-12-memory-leak-limits": {
      "description": "A deployment is gradually consuming more and more memory over time. Eventually the pod's memory usage exceeds the node's available memory and the Linux OOM killer starts killing other pods on the same node — including unrelated, healthy workloads. The leaky pod has no memory limit set, so it grows without bound.",
      "hint1": "Watch the memory usage of the leaky pod over time: `kubectl top pod -n k8smissions -l app=leaky`. Compare the memory shown against any limits: `kubectl get deployment leaky-app -n k8smissions -o yaml | grep -A8 resources`",
      "hint2": "The pod shows growing memory usage and the `resources` section only has `requests` — there are no `limits`. Without a memory limit, the container can consume all available memory on the node. When node memory is exhausted, the OOM killer terminates processes — often killing neighboring pods.",
      "hint3": "The `leaky-app` container has no `limits` defined. Add `limits.memory: 512Mi` to cap the container's memory usage. When it hits the limit, the OOM killer restarts only this container, not neighboring pods.",
      "reveal": "## What went wrong\n\nWithout a memory limit, the container's cgroup has no memory ceiling. As memory leaks accumulate, the container grows until the entire node runs out of memory. At that point, the Linux OOM killer selects processes to kill based on OOM score — often killing other pods, not the leaky one. The leaky pod (having the most memory) actually has a negative OOM adjustment and may survive while innocent pods are killed.\n\n## Fix\n\n```yaml\nresources:\n  requests:\n    cpu: 100m\n    memory: 256Mi\n  limits:\n    cpu: 500m\n    memory: 512Mi\n```\n\n## Why this matters\n\nMemory limits enforce container-level isolation. When a container exceeds its memory limit, the kernel OOM-kills only that container — Kubernetes then restarts it based on the `restartPolicy`. This is contained blast radius vs. node-wide OOM. Trade-offs:\n- Setting limits too low: frequent OOMKills even for healthy memory growth\n- Setting limits too high: insufficient isolation from memory leaks\n\nA memory leak should be fixed, not just contained — but limits prevent blast radius while the fix ships. Monitor `container_oom_events_total` in Prometheus. Consider setting `limits.memory` to 2× `requests.memory` for containers with known variable memory usage."
    },
    "level-13-slow-rollout": {
      "description": "Deploying a new image to a 100-replica deployment is taking over 30 minutes. The rolling update strategy has `maxSurge: 0` and `maxUnavailable: 1` — meaning only one pod is replaced at a time, sequentially. Each pod replacement takes 15–20 seconds, so 100 replacements take 25–33 minutes.",
      "hint1": "Watch the rollout: `kubectl rollout status deployment/slow-deploy -n k8smissions`. Count how many pods are updated at each step — you should see exactly 1 pod updating at a time.",
      "hint2": "Check the rolling update strategy: `kubectl get deployment slow-deploy -n k8smissions -o yaml | grep -A5 rollingUpdate`. With `maxSurge: 0` and `maxUnavailable: 1`, the deployment creates no extra capacity and removes/replaces exactly 1 pod at a time.",
      "hint3": "The strategy has `maxSurge: 0` and `maxUnavailable: 1`. Change to `maxSurge: 10` and `maxUnavailable: 0` to replace 10 pods in parallel while keeping all original pods running until replacements are ready.",
      "reveal": "## What went wrong\n\n`maxSurge: 0, maxUnavailable: 1` produces a serialized replacement: terminate 1 old pod → wait for new pod to be Ready → repeat 99 more times. With 100 replicas and ~20 seconds per pod, this takes 33 minutes. This also means every deployment keeps 99/100 capacity during the update, which is cautious but extremely slow.\n\n## Fix\n\n```yaml\nstrategy:\n  type: RollingUpdate\n  rollingUpdate:\n    maxSurge: 10\n    maxUnavailable: 0\n```\n\nNow 10 new pods are created simultaneously (10% surge), and when they become Ready, 10 old pods are terminated. The rollout takes ~100/10 = 10 sequential batches instead of 100 sequential pods.\n\n## Why this matters\n\n`maxSurge` and `maxUnavailable` control the rollout rate and resource cost:\n- `maxSurge: N`: up to N extra pods above `replicas` during rollout (requires extra capacity)\n- `maxUnavailable: N`: up to N pods can be unavailable at once (risky if N is large)\n- `maxSurge: 25%, maxUnavailable: 25%` is a common balanced default\n\nFor critical services: set `maxUnavailable: 0` (no capacity reduction) and `maxSurge` as high as your cluster can absorb. For resource-constrained clusters: increase `maxUnavailable` to avoid needing surge capacity."
    },
    "level-14-resource-quota-bust": {
      "description": "New pods are stuck in `Pending` with 'exceeded quota: compute-resources'. The namespace has a ResourceQuota and the total CPU/memory requested by all pods has hit the ceiling. Scaling up deployments or deploying new workloads is blocked until quota is increased or existing workloads are scaled down.",
      "hint1": "Check namespace quota usage: `kubectl describe resourcequota compute-resources -n k8smissions`. Look at the `Used` vs `Hard` values for `requests.cpu` and `requests.memory`.",
      "hint2": "You should see the `Used` values at or near the `Hard` limits. The quota has `requests.cpu: 4` and `requests.memory: 8Gi` but the sum of all pod requests exceeds these values. Any new pod or scale-up is rejected.",
      "hint3": "The ResourceQuota has `requests.cpu: 4` which is too low for the current workload. Increase it to `requests.cpu: 8` and `requests.memory: 16Gi` (and the corresponding limits) to give the namespace room to grow.",
      "reveal": "## What went wrong\n\nThe namespace ResourceQuota enforces hard limits on the total resources all pods in the namespace can request. When the sum of all pod `requests.cpu` exceeds `hard.requests.cpu`, new pod creations fail at admission with a quota exceeded error.\n\n## Fix\n\n```yaml\nspec:\n  hard:\n    requests.cpu: '8'\n    requests.memory: 16Gi\n    limits.cpu: '16'\n    limits.memory: 32Gi\n```\n\n## Why this matters\n\nResourceQuotas are the primary mechanism for multi-tenant namespace isolation — preventing one team's workloads from consuming the entire cluster. Common pitfalls:\n1. **Limits required**: if a quota for `limits.cpu` exists, every container in the namespace must set `limits.cpu` — pods without limits are rejected\n2. **Quota ≠ capacity**: quota limits resource *requests*, not actual usage. Setting quota to cluster capacity doesn't prevent overcommit.\n3. **Quota scope**: quotas can be scoped (e.g., only count `BestEffort` pods) to allow different rules for different QoS classes\n4. **PriorityClass quotas**: use `scopeSelector: matchExpressions: [{operator: In, scopeName: PriorityClassName, values: [high-priority]}]` to set separate quotas per priority"
    },
    "level-15-network-policy-latency": {
      "description": "After adding a NetworkPolicy to restrict pod-to-pod traffic, latency increased by 2–3ms per hop. The NetworkPolicy is correct and correctly limits traffic — but the network plugin is using an iptables-based implementation that scales poorly with rule count. The fix is to switch to a Cilium eBPF data plane that provides O(1) rule lookup.",
      "hint1": "Measure latency before and after the NetworkPolicy: `kubectl exec -n k8smissions <frontend-pod> -- time curl http://my-app:8080/ping`. Check what network plugin is running: `kubectl get pods -n kube-system | grep cilium`",
      "hint2": "With iptables-based NetworkPolicy enforcement (e.g., kube-proxy iptables mode), each new NetworkPolicy rule adds iptables chain entries that are traversed linearly. With hundreds of policies, every packet traverses hundreds of iptables rules — adding measurable latency.",
      "hint3": "Add Cilium-specific annotations to the NetworkPolicy: `policy-engine: cilium-ebpf` and `optimization: O(1)-lookup-enabled`. These annotations signal to Cilium to use its eBPF-based policy enforcement, which uses hash-map lookups instead of linear iptables chain traversal.",
      "reveal": "## What went wrong\n\niptables-based NetworkPolicy enforcement stores rules as iptables chains, which are traversed linearly for every packet. As the number of NetworkPolicies and pod count grows, each packet must traverse more rules — O(n) lookup time. At scale (hundreds of pods, dozens of policies), this adds measurable per-hop latency.\n\n## Fix\n\n```yaml\nmetadata:\n  annotations:\n    policy-engine: cilium-ebpf\n    optimization: O(1)-lookup-enabled\n```\n\n## Why this matters\n\nCilium uses eBPF (extended Berkeley Packet Filter) maps for NetworkPolicy enforcement instead of iptables:\n- **iptables**: O(n) rule traversal — latency grows linearly with policy count\n- **eBPF hash maps**: O(1) lookup — constant time regardless of policy count\n\nAt scale (1000+ pods, 100+ NetworkPolicies), iptables enforcement can add 5–10ms latency per connection and consume significant CPU for rule updates. Cilium's eBPF data plane maintains sub-millisecond enforcement overhead. For clusters running Cilium, always prefer Cilium-specific features (CiliumNetworkPolicy) for advanced policy rules that go beyond the standard Kubernetes NetworkPolicy API."
    },
    "level-16-chaos-readiness": {
      "description": "During chaos engineering testing, simulating the failure of a single node took down the application entirely. The deployment only has 2 replicas and no topology spread constraints — both pods happened to be scheduled on the same node. When that node failed, all capacity was lost simultaneously.",
      "hint1": "Check pod distribution across nodes: `kubectl get pods -n k8smissions -l app=resilient -o wide`. How many unique nodes are in the NODE column?",
      "hint2": "Both pods may be on the same node — or at best on two out of many available nodes. With only 2 replicas, one node failure eliminates at least half the capacity. The deployment has no constraints forcing pods to spread across nodes.",
      "hint3": "Increase `replicas` from 2 to 5. Add `topologySpreadConstraints` with `topologyKey: kubernetes.io/hostname` and `maxSkew: 1` to force pods to spread evenly across nodes — so no single node failure can take down more than ~20% of capacity.",
      "reveal": "## What went wrong\n\nWith 2 replicas and no spread constraints, the scheduler can place both pods on the same node (or place them badly balanced). A single node failure can eliminate all replicas. Even with both pods on different nodes, losing one node loses 50% capacity.\n\n## Fix\n\n```yaml\nspec:\n  replicas: 5\n  template:\n    spec:\n      topologySpreadConstraints:\n      - maxSkew: 1\n        topologyKey: kubernetes.io/hostname\n        whenUnsatisfiable: DoNotSchedule\n        labelSelector:\n          matchLabels:\n            app: resilient\n```\n\n## Why this matters\n\n`topologySpreadConstraints` tells the scheduler to distribute pods across topology domains (nodes, zones, regions) such that the count difference between any two domains (`maxSkew`) is at most the configured value. With 5 pods across 5 nodes, `maxSkew: 1` means no node has more than 1 more pod than any other — a single node failure leaves 4/5 capacity intact.\n\n`whenUnsatisfiable: DoNotSchedule` refuses to place pods that would violate the constraint. Use `ScheduleAnyway` to prefer spreading without hard enforcement. For zone-level HA (surviving AZ failures), use `topologyKey: topology.kubernetes.io/zone` instead."
    },
    "level-17-slo-breach": {
      "description": "An application with a 99.9% availability SLO is regularly breaching its error budget. Instances crash due to application bugs, but the liveness probe's `failureThreshold: 10` means Kubernetes waits 100 seconds (10 checks × 10 seconds) before restarting the container. Each crash consumes 100 seconds of the monthly error budget.",
      "hint1": "Check the current liveness probe configuration: `kubectl get deployment slo-app -n k8smissions -o yaml | grep -A10 livenessProbe`",
      "hint2": "The liveness probe has `periodSeconds: 10` and `failureThreshold: 10`, meaning a crashed container can fail 10 consecutive checks before being restarted — that's 100 seconds of downtime per crash. The annotation shows a 99.9% SLO with only 43.8 minutes/month of error budget.",
      "hint3": "The `failureThreshold: 10` allows 100 seconds (10 failures × 10 seconds) of downtime before restart. Reduce it to `3` to trigger a restart after just 30 seconds of failures, significantly reducing per-incident downtime.",
      "reveal": "## What went wrong\n\n`failureThreshold: 10` × `periodSeconds: 10` = 100 seconds before the container is restarted. At 99.9% availability, the monthly error budget is 43.8 minutes. Even a single incident consuming 100 seconds of downtime eats ~3.8% of the monthly budget. Multiple incidents per month quickly exhaust it.\n\n## Fix\n\n```yaml\nlivenessProbe:\n  httpGet:\n    path: /health/live\n    port: 8080\n  initialDelaySeconds: 5\n  periodSeconds: 10\n  failureThreshold: 3\n```\n\nDetection-to-restart time: 3 × 10s = 30 seconds maximum.\n\n## Why this matters\n\nLiveness probe tuning directly impacts availability SLO compliance:\n- `failureThreshold: 3, periodSeconds: 10` → max 30s to detect and restart\n- `failureThreshold: 10, periodSeconds: 10` → max 100s to detect and restart\n\nThe right `failureThreshold` depends on your workload. Too low (1–2) causes false-positive restarts on transient GC pauses or slow responses. Too high (10+) delays recovery from real crashes. Three is a common production default: it tolerates a single missed check without restarting, but acts quickly on sustained failures. Remember: liveness probes kill and restart containers — readiness probes (level 6) stop traffic routing. Use both together."
    },
    "level-18-performance-finale": {
      "description": "Three independent performance problems are active simultaneously: a pod is generating excessive DNS queries using short names without ndots tuning, an API deployment is heavily CPU-throttled because its limit equals its request, and a 50-replica deployment is rolling out at a crawl because maxSurge is zero. Identify and fix all three.",
      "hint1": "Start with observation: `kubectl top pods -n k8smissions` for CPU usage, `kubectl rollout status deployment/slow-rollout -n k8smissions` for rollout progress, and `kubectl exec dns-pod -n k8smissions -- cat /etc/resolv.conf` for DNS config. Identify all three problems before touching anything.",
      "hint2": "Problem 1: `dns-pod` uses short name `api-svc` with default ndots:5 — 5 failed searches per lookup. Problem 2: `throttled-api` has `limits.cpu: 200m == requests.cpu: 200m` — zero burst headroom, immediate throttling. Problem 3: `slow-rollout` has `maxSurge: 0, maxUnavailable: 1` — 50 sequential pod replacements.",
      "hint3": "Three fixes: (1) add `dnsConfig.options.ndots: 2` to dns-pod and use FQDN in the command; (2) increase `throttled-api` CPU limit to `2000m` while keeping request at `200m`; (3) change `slow-rollout` strategy to `maxSurge: 10, maxUnavailable: 0`.",
      "reveal": "## What went wrong\n\nThree independent performance issues compounding:\n1. **DNS amplification**: short name `api-svc` generates 5 DNS queries per lookup (ndots:5 default searches all suffixes)\n2. **CPU throttling**: `limits.cpu == requests.cpu` leaves no burst capacity, causing CFS throttle on any load spike\n3. **Serial rollout**: `maxSurge: 0, maxUnavailable: 1` with 50 replicas = 50 sequential replacements\n\n## Fix\n\n```yaml\n# Fix 1: DNS pod\nspec:\n  dnsConfig:\n    options:\n    - name: ndots\n      value: '2'\n  containers:\n  - command:\n    - /bin/sh\n    - -c\n    - while true; do nslookup api-svc.k8smissions.svc.cluster.local; sleep 1; done\n---\n# Fix 2: API deployment CPU limit\nresources:\n  requests:\n    cpu: 200m\n  limits:\n    cpu: 2000m\n---\n# Fix 3: Rollout strategy\nstrategy:\n  rollingUpdate:\n    maxSurge: 10\n    maxUnavailable: 0\n```\n\n## Why this matters\n\nPerformance problems rarely announce themselves clearly. The skill is knowing where to look:\n- **DNS**: check ndots, look at kube-dns CPU, count queries/second\n- **CPU throttling**: `container_cpu_cfs_throttled_seconds_total` in Prometheus; compare limit vs. usage\n- **Rollout velocity**: `kubectl rollout status` shows progress; check `maxSurge`/`maxUnavailable` when it's slow\n\nEach fix is independent — order doesn't matter. But in production, fix the highest-impact issue first: CPU throttling causes user-visible latency; slow rollouts create deployment risk windows; DNS overhead adds latency at scale."
    }
  }
}
