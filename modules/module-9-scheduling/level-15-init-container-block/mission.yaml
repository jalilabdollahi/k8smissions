{
  "name": "Init Loop",
  "description": "The main application container never starts. The pod has been stuck in `Init` state for over an hour. An init container is polling a database host that doesn't exist — so the connectivity check loops forever, and the main container waits indefinitely for the init phase to complete.",
  "objective": "An init container is failing and retrying — fix the init container command so it exits 0 and allows the main container to start",
  "xp": 300,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "init containers",
    "InitCrashLoopBackOff",
    "pod phases",
    "sequencing"
  ],
  "module": "module-9-scheduling",
  "level": "level-15-init-container-block"
}
