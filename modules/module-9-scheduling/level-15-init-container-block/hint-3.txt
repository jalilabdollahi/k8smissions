The init container's command polls `non-existent-db-host` which doesn't exist. Change the command to `echo 'DB check skipped'; exit 0` to let the init phase complete successfully and unblock the main container.