The ResourceClaim references `resourceClassName: non-existent-fpga-class` but no such ResourceClass exists. Create a ResourceClass named `fpga-class` with the correct driver name, then update the ResourceClaim to reference it.