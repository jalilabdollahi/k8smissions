{
  "name": "No Room",
  "description": "A pod is stuck in Pending despite the cluster appearing to have capacity. The resource request is the problem — the pod asks for 100Gi of memory, which exceeds what any single node can provide. Kubernetes must schedule a pod entirely on one node; it cannot split a pod across nodes.",
  "objective": "The pod requests more memory than any single node can provide — reduce requests to fit within available node capacity",
  "xp": 275,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "resource requests",
    "node capacity",
    "allocatable",
    "scheduling",
    "bin packing"
  ],
  "module": "module-9-scheduling",
  "level": "level-8-resource-fit"
}
