The events should say 'Insufficient memory' on all nodes. Check each node's allocatable memory: `kubectl describe node | grep -E 'Name:|memory'`. Compare against the pod's `resources.requests.memory`.