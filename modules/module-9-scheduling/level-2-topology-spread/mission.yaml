{
  "name": "Uneven Spread",
  "description": "All pods from a 4-replica Deployment are landing on the same node. If that node fails, the entire service goes down simultaneously. There is no constraint telling the scheduler to spread pods across nodes — so it packs them wherever there is space.",
  "objective": "Add a TopologySpreadConstraint to evenly distribute pods across nodes with maxSkew: 1",
  "xp": 300,
  "difficulty": "intermediate",
  "expected_time": "12m",
  "concepts": [
    "TopologySpreadConstraint",
    "maxSkew",
    "topologyKey",
    "topology spread",
    "HA"
  ],
  "module": "module-9-scheduling",
  "level": "level-2-topology-spread"
}
