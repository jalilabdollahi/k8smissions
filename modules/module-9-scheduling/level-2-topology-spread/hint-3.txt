The pod spec has no `topologySpreadConstraints` block. Add one with `maxSkew: 1` and `topologyKey: kubernetes.io/hostname` to enforce that no node has more than 1 extra pod compared to any other node.