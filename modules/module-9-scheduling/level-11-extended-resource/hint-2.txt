The events say '0 nodes have sufficient my.company/fpga'. Check what extended resources are actually available on nodes: `kubectl describe node | grep -A20 'Allocatable'`. Look for any `*.io/fpga` or similar resources.