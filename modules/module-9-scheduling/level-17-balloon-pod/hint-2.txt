You should see most CPU and memory requests are consumed. Find which pod is responsible: `kubectl get pods -n k8smissions -o custom-columns=NAME:.metadata.name,CPU:.spec.containers[0].resources.requests.cpu,MEM:.spec.containers[0].resources.requests.memory`