The `balloon-pod` has `resources.requests.cpu: 2000m` and `resources.requests.memory: 4Gi` on a `pause` container that consumes zero real resources. Reduce these to minimal values — `100m` CPU and `128Mi` memory — to free up scheduling capacity.