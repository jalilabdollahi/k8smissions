{
  "name": "Scheduler Deadlock",
  "description": "All pods in the cluster are stuck in Pending — even tiny pods with minimal resource requests that should schedule immediately. Nodes have available capacity. This is not a resource problem. A misconfigured admission webhook is blocking every pod creation request cluster-wide.",
  "objective": "A misconfigured admission webhook is rejecting all scheduling decisions — identify and disable the broken webhook",
  "xp": 375,
  "difficulty": "advanced",
  "expected_time": "20m",
  "concepts": [
    "admission webhook",
    "ValidatingWebhook",
    "scheduling block",
    "webhook timeout",
    "failurePolicy"
  ],
  "module": "module-9-scheduling",
  "level": "level-13-pending-deadlock"
}
