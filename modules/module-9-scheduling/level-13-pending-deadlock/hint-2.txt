Look at the webhook's `failurePolicy` field: `kubectl get validatingwebhookconfiguration strict-validator -o yaml | grep failurePolicy`. If the webhook service is unreachable and `failurePolicy: Fail`, every pod creation is rejected.