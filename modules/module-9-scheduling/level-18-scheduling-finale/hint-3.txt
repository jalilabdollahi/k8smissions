Three fixes: (1) remove `nodeSelector: {team: team-a}` from team-a-pod since no node has that label; (2) change team-b-deploy's `requiredDuringScheduling` anti-affinity to `preferred` so 5 replicas can fit on 2 nodes; (3) remove `priorityClassName: non-existent-priority` from team-c-pod so it can be admitted.