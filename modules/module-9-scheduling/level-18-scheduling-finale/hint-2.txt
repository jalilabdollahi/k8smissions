Each pod has a different failure reason: team-a sees 'didn't match node selector', team-b sees 'didn't match pod anti-affinity rules' (not enough nodes), team-c sees an API rejection for the unknown PriorityClass. Fix them independently — each has a different root cause.