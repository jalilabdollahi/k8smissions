The pod is using `low-priority` class. Check what PriorityClasses exist: `kubectl get priorityclass`. The `critical-workload` pod should be using a high-priority class so the scheduler will preempt lower-priority pods to schedule it.