{
  "name": "Queue Jumped",
  "description": "A critical workload pod is stuck Pending while lower-priority development pods consume all available CPU and memory. The critical pod is assigned the wrong PriorityClass — it has lower priority than the pods it needs to displace, so the scheduler never preempts them to make room.",
  "objective": "The critical pod has the wrong PriorityClass — assign it the high-priority class so it can preempt lower-priority pods",
  "xp": 325,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "PriorityClass",
    "preemption",
    "pod priority",
    "scheduling",
    "resource contention"
  ],
  "module": "module-9-scheduling",
  "level": "level-5-priority-preemption"
}
