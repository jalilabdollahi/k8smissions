{
  "name": "Wrong Scheduler",
  "description": "A machine learning workload pod is being scheduled by the default Kubernetes scheduler instead of the custom ML-aware scheduler deployed for these workloads. Without specifying which scheduler to use, every pod defaults to `default-scheduler` — bypassing all GPU-topology and ML-specific placement logic.",
  "objective": "The pod uses the default scheduler instead of the custom 'ml-scheduler' — set the schedulerName field",
  "xp": 300,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "schedulerName",
    "custom scheduler",
    "multiple schedulers",
    "pod spec"
  ],
  "module": "module-9-scheduling",
  "level": "level-12-scheduler-name"
}
