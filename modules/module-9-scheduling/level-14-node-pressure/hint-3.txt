The `memory-hog` pod runs `dd if=/dev/zero bs=1M count=1000` which allocates ~1GB, but its `resources.requests.memory` is only `50Mi` and `limits.memory` is `2Gi`. Change the command to `sleep 3600` and reduce the memory limit to `128Mi`.