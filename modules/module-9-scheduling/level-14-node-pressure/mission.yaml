{
  "name": "Pressure Evictions",
  "description": "Pods are being unexpectedly evicted from a node that appears healthy — CPU utilization is low but memory pods keep disappearing. One pod is allocating nearly 1GB of memory using a `dd` command while declaring only 50Mi in its requests. This mismatch triggers the kubelet's eviction manager.",
  "objective": "Node memory pressure is triggering kubelet eviction — the node has too many high-memory pods. Remove or reschedule some pods",
  "xp": 325,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "node pressure",
    "eviction",
    "kubelet",
    "memory pressure",
    "eviction thresholds"
  ],
  "module": "module-9-scheduling",
  "level": "level-14-node-pressure"
}
