The events show 'node(s) had untolerated taint' or 'didn't match node selector'. Look at the pod spec — it has both a `nodeSelector: {node-type: spot}` and tolerations only for spot nodes. On-demand nodes have their own taint that isn't tolerated.