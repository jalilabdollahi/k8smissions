Remove the nodeSelector (or make it a soft preference) and add toleration for on-demand nodes:
  tolerations:
  - key: spot
    operator: Equal
    value: true
    effect: NoSchedule
  - key: on-demand
    operator: Equal
    value: true
    effect: NoSchedule
