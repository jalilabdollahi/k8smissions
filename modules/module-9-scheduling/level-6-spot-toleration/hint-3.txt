Two things need fixing: the `nodeSelector: {node-type: spot}` blocks rescheduling to on-demand nodes, and there is no toleration for the `on-demand=true:NoSchedule` taint. Remove the nodeSelector and add the on-demand toleration.