{
  "name": "Hot Node",
  "description": "One node is running 80% of all pods while other nodes sit mostly empty. The Descheduler is deployed to handle this kind of imbalance by evicting pods from overloaded nodes so they can reschedule to underutilized ones — but it's been configured with the rebalancing strategy disabled.",
  "objective": "Configure the Descheduler policy to evict pods from overloaded nodes and allow rebalancing",
  "xp": 350,
  "difficulty": "advanced",
  "expected_time": "15m",
  "concepts": [
    "Descheduler",
    "LowNodeUtilization",
    "pod eviction",
    "rebalancing",
    "cluster efficiency"
  ],
  "module": "module-9-scheduling",
  "level": "level-9-descheduler-config"
}
