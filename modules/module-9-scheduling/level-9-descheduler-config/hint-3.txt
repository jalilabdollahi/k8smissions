The `LowNodeUtilization` strategy has `enabled: false`. Change it to `enabled: true` to allow the descheduler to evict pods from nodes above the `targetThresholds` and rebalance them to nodes below `thresholds`.