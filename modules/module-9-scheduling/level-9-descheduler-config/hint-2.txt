You should see `enabled: false` on the `LowNodeUtilization` strategy. This means the descheduler never acts, even though the policy thresholds are configured. The `enabled` field is the on/off switch for the entire strategy.