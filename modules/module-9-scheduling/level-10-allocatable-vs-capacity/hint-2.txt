You should see `Capacity.memory` (e.g., 6Gi) and `Allocatable.memory` (e.g., something smaller, like 5.5Gi or less after kubelet reserves). The pod's `requests.memory: 5Gi` may exceed the allocatable amount even if it's within capacity.