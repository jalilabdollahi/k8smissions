The pod's `resources.requests.memory` is set too high relative to the node's *allocatable* memory (not its total capacity). Reduce the request to a value comfortably within the allocatable amount — like `1Gi`.