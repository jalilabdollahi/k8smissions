{
  "name": "Phantom Capacity",
  "description": "A pod requests 5Gi of memory but can't schedule even though the node shows 6Gi total capacity. The scheduler doesn't use the raw capacity number — it uses allocatable memory, which is capacity minus what the kubelet and system processes have reserved. The pod's request exceeds the allocatable amount.",
  "objective": "Node capacity includes system and kubelet reserved resources — calculate actual allocatable memory to size pods correctly",
  "xp": 325,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "allocatable",
    "capacity",
    "reserved resources",
    "kubelet",
    "kube-reserved"
  ],
  "module": "module-9-scheduling",
  "level": "level-10-allocatable-vs-capacity"
}
