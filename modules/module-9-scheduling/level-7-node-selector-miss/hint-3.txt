The pod's `nodeSelector` has `accelerator-type: nvidia-v100` but no node has this label. Remove the `nodeSelector` block so the pod can schedule on any available node (or label a node correctly first).