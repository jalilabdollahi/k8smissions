{
  "name": "Sudden Eviction",
  "description": "Running pods are being evicted unexpectedly from a node that just had a maintenance taint applied. The pod has no toleration for the taint — so when the `NoExecute` taint lands on the node, Kubernetes evicts it immediately with no grace window.",
  "objective": "A NoExecute taint was added to the node — add a toleration with tolerationSeconds to allow a graceful drain window",
  "xp": 275,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "taints",
    "NoExecute",
    "tolerationSeconds",
    "eviction",
    "graceful shutdown"
  ],
  "module": "module-9-scheduling",
  "level": "level-1-taint-eviction"
}
