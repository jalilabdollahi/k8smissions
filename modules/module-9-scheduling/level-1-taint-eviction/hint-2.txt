You should see a `NoExecute` taint like `maintenance=draining:NoExecute`. Look at the pod spec — it has no `tolerations` block at all. Without a toleration, a `NoExecute` taint causes instant eviction.