You should see `WATCH_NAMESPACE: operators`. The operator uses this value to scope its informers. Any CR in a different namespace (like `k8smissions`) is outside the watch scope and never triggers a reconciliation.