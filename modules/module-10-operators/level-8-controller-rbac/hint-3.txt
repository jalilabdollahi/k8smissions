The ClusterRole only grants access to `example.com/workloads`. It's missing rules for: `"" / pods,services,configmaps` (core API), `apps / deployments`, and `example.com / workloads/status` (the status subresource).