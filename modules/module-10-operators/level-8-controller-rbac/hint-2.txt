The logs will show forbidden errors for pods, services, or deployments. Look at the current ClusterRole: `kubectl get clusterrole my-operator-role -o yaml`. Compare the rules against what the operator actually manages.