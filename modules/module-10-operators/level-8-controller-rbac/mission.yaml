{
  "name": "Permission Denied in Controller",
  "description": "The operator is running but its logs show '403 Forbidden' errors when it tries to list pods. The operator's ClusterRole only grants access to its own CRD resources — it's missing permissions for the standard Kubernetes resources it needs to manage: Pods, Deployments, Services, ConfigMaps, and the status subresource.",
  "objective": "The operator's ServiceAccount lacks RBAC permissions to list and watch Pods — fix the ClusterRole",
  "xp": 300,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "RBAC",
    "ServiceAccount",
    "ClusterRole",
    "ClusterRoleBinding",
    "operator permissions"
  ],
  "module": "module-10-operators",
  "level": "level-8-controller-rbac"
}
