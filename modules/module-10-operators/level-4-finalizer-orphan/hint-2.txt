The object has `finalizers: [database.example.com/cleanup]`. This finalizer was registered by the database operator, which is supposed to run cleanup logic and then remove the finalizer. But the operator is gone — no code will ever clear it.