{
  "name": "Orphaned Controller",
  "description": "Deleting a custom resource takes forever — or it enters Terminating state and stays there indefinitely. The CR has a finalizer registered by an operator that no longer exists. Since no controller will ever clear the finalizer, the object is stuck in Terminating permanently.",
  "objective": "The operator's finalizer logic ran successfully but forgot to actually clean up the backend resources before clearing the finalizer",
  "xp": 375,
  "difficulty": "advanced",
  "expected_time": "18m",
  "concepts": [
    "finalizers",
    "operator cleanup",
    "garbage collection",
    "ownerReferences",
    "controller"
  ],
  "module": "module-10-operators",
  "level": "level-4-finalizer-orphan"
}
