The operator Deployment has `initContainers: []`. Add an init container that polls `kubectl get crd workloads.example.com` in a loop until it succeeds — this blocks the main operator container from starting until the CRD is registered.