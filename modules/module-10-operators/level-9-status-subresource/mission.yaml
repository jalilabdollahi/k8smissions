{
  "name": "Status Not Updating",
  "description": "Status updates on a custom resource silently succeed but the status never changes. `kubectl patch` returns no error, but `kubectl get` shows the old status. The CRD has a `status` field defined in its schema but is missing the subresource configuration that makes status updates actually work.",
  "objective": "The CRD doesn't have the status subresource enabled — status updates require a separate subresource config",
  "xp": 325,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "status subresource",
    "CRD",
    "operator status",
    "kubectl patch"
  ],
  "module": "module-10-operators",
  "level": "level-9-status-subresource"
}
