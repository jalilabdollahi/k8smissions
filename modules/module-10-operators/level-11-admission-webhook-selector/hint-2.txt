The DNS pods may show `Pending` or errors. Look at the webhook configuration: `kubectl get mutatingwebhookconfiguration pod-mutator -o yaml | grep namespaceSelector`. There is no selector — the webhook intercepts pods from every namespace.