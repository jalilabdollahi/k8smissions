{
  "name": "System Pod Blocked",
  "description": "After installing a MutatingWebhookConfiguration, kube-dns pods fail to start and cluster DNS breaks. The webhook applies to all namespaces including `kube-system` — when the webhook service is restarted or slow, DNS pods can't be created, which breaks DNS, which breaks the webhook service itself. It's a deadlock.",
  "objective": "The MutatingWebhookConfiguration applies to all namespaces including kube-system — add a namespaceSelector to exclude system namespaces",
  "xp": 350,
  "difficulty": "advanced",
  "expected_time": "15m",
  "concepts": [
    "MutatingWebhook",
    "namespaceSelector",
    "kube-system",
    "DNS",
    "webhook scope"
  ],
  "module": "module-10-operators",
  "level": "level-11-admission-webhook-selector"
}
