Only `v1` is listed as `served: true, storage: true`. But objects created with the old `v1alpha1` API are still in etcd stored under that version. Without `v1alpha1` in the CRD, the API server can't serve them and returns an error.