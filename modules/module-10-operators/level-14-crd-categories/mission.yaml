{
  "name": "Not in List",
  "description": "`kubectl get all` does not show custom `App` resources even though the CRD is installed and resources exist. The CRD is not included in the `all` aggregated category — which is how `kubectl get all` discovers which resource types to include in its output.",
  "objective": "The CRD lacks a 'categories' field that includes 'all' — add it to make CRs appear in kubectl get all",
  "xp": 275,
  "difficulty": "intermediate",
  "expected_time": "8m",
  "concepts": [
    "CRD",
    "categories",
    "kubectl get all",
    "shortNames",
    "API discovery"
  ],
  "module": "module-10-operators",
  "level": "level-14-crd-categories"
}
