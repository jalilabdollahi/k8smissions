{
  "name": "Slow Admission",
  "description": "Pod creation is extremely slow — every `kubectl apply` for a pod takes 30+ seconds before the pod even enters Pending. The admission webhook has `timeoutSeconds: 30`, meaning the API server waits up to 30 seconds for the webhook's response before timing out. A slow external API call inside the webhook is causing the delay.",
  "objective": "The admission webhook has a long timeout and is making slow external API calls — reduce timeoutSeconds",
  "xp": 325,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "admission webhook",
    "timeoutSeconds",
    "performance",
    "latency",
    "webhook optimization"
  ],
  "module": "module-10-operators",
  "level": "level-13-webhook-timeout"
}
