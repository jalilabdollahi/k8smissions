You should see `generation: 5` in metadata but `observedGeneration: 1` in status. The operator should update `observedGeneration` to match `generation` after successfully reconciling — this allows it to skip reconciliation when nothing has changed.