{
  "name": "Handshake Refused",
  "description": "New pods are being rejected with 'x509: certificate signed by unknown authority'. The admission webhook has a hardcoded `caBundle` in its configuration — but the certificate it references has either expired or was replaced. The API server's TLS handshake with the webhook service fails.",
  "objective": "The admission webhook TLS certificate has expired or is self-signed without being trusted — update the caBundle in the webhook config",
  "xp": 375,
  "difficulty": "advanced",
  "expected_time": "15m",
  "concepts": [
    "admission webhook",
    "TLS",
    "caBundle",
    "x509",
    "cert-manager"
  ],
  "module": "module-10-operators",
  "level": "level-5-webhook-cert"
}
