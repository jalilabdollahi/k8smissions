The `caBundle` field is hardcoded and stale. Remove it and add the annotation `cert-manager.io/inject-ca-from: operators/webhook-cert` to let cert-manager automatically inject and rotate the correct CA bundle.