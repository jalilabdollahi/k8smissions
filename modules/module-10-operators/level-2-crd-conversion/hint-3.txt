The CRD needs two changes: set `storage: true` on v1beta1 (and `storage: false` on v1alpha1), and change `conversion.strategy` from `None` to `Webhook` with a webhook pointing to the conversion service.