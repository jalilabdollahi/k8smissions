{
  "name": "Version Migration",
  "description": "A CRD has two versions — `v1alpha1` and `v1beta1`. Resources created with v1alpha1 are invisible when querying the v1beta1 API. The CRD has `conversion.strategy: None`, which means Kubernetes makes no attempt to convert objects between versions. The storage version also needs to move to v1beta1.",
  "objective": "The CRD has two versions but no conversion webhook — stored v1alpha1 resources won't auto-convert to v1beta1 format",
  "xp": 375,
  "difficulty": "advanced",
  "expected_time": "18m",
  "concepts": [
    "CRD versioning",
    "conversion webhook",
    "hub version",
    "storage version",
    "CRD migration"
  ],
  "module": "module-10-operators",
  "level": "level-2-crd-conversion"
}
