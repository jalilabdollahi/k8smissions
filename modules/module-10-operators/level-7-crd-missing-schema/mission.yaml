{
  "name": "Unvalidated Resource",
  "description": "The API accepts completely invalid values in custom resource fields. Creating a `Workload` with `replicas: -999` succeeds without any error. The CRD uses `x-kubernetes-preserve-unknown-fields: true` with no schema — it accepts any structure, making validation impossible.",
  "objective": "The CRD has no OpenAPI validation schema — add proper schema validation to reject invalid values",
  "xp": 325,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "CRD",
    "OpenAPI schema",
    "validation",
    "structural schema",
    "x-kubernetes-validations"
  ],
  "module": "module-10-operators",
  "level": "level-7-crd-missing-schema"
}
