The CRD schema uses `x-kubernetes-preserve-unknown-fields: true` which disables all validation. Replace it with explicit property definitions including `required` fields and `minimum`/`maximum` constraints on `replicas`.