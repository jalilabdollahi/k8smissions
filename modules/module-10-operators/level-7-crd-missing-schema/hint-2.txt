Look at the CRD schema: `kubectl get crd workloads.example.com -o yaml | grep -A10 openAPIV3Schema`. You should see only `x-kubernetes-preserve-unknown-fields: true` with no property definitions or constraints.