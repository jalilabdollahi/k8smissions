The three problems: (1) webhook has `failurePolicy: Fail` with no namespaceSelector — blocks kube-system; (2) CRD only lists v2 but objects are stored as v1 — they're unreadable; (3) operator has `--leader-elect=false` with 2 replicas — split-brain and resource flapping.