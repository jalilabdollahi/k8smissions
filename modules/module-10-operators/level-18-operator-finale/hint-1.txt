Start broad: `kubectl get pods -A | grep -v Running` to see what's failing, then `kubectl get crd apps.example.com -o yaml | grep versions` and `kubectl get deployment my-operator -n operators -o yaml | grep leader`. List all three problems before fixing.