{
  "name": "Operator Meltdown",
  "description": "Three simultaneous operator failures are active: a webhook is blocking all pod creation in system namespaces with a bad failure policy, a CRD is missing the old version needed to read existing stored objects, and the operator has two replicas in split-brain because leader election is off. All three must be fixed independently.",
  "objective": "Fix all three: restore webhook to safe mode, fix CRD version configuration, and enable leader election on the operator",
  "xp": 425,
  "difficulty": "advanced",
  "expected_time": "25m",
  "concepts": [
    "admission webhook",
    "CRD versioning",
    "leader election",
    "operator",
    "multi-failure"
  ],
  "module": "module-10-operators",
  "level": "level-18-operator-finale"
}
