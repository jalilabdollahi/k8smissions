You should see `--leader-elect=false`. With two replicas and no leader election, both pods run the full reconciliation loop simultaneously. Each sees the other's updates as 'drift' and reverts them — causing the resource flapping you observe.