{
  "name": "No Endpoints",
  "description": "Pod backend-pod is Running but the Service backend-service has no endpoints. Requests to the Service are failing. Run kubectl get endpoints backend-service -n k8smissions.",
  "objective": "The service selector label value doesn't match the pod label — fix the mismatch so endpoints are populated",
  "xp": 200,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "ClusterIP service",
    "label selectors",
    "endpoints",
    "kubectl get ep"
  ],
  "module": "module-3-networking",
  "level": "level-1-service-selector"
}
