{
  "name": "NetworkPolicy Too Strict",
  "description": "Pod frontend is Running and trying to reach the backend API every 5 seconds. Pod backend is Running. But requests are being silently dropped. Run kubectl logs frontend -n k8smissions.",
  "objective": "The NetworkPolicy blocks all ingress to the backend — add a rule that allows traffic from the frontend",
  "xp": 250,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "NetworkPolicy",
    "default-deny",
    "ingress rules",
    "podSelector",
    "namespaceSelector"
  ],
  "module": "module-3-networking",
  "level": "level-5-networkpolicy"
}
