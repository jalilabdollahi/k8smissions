{
  "name": "404 Not Found",
  "description": "Ingress app-ingress exists and the backend pod is Running. But every request to the Ingress returns 404. Run kubectl describe ingress app-ingress -n k8smissions.",
  "objective": "The Ingress rules have a typo in the service name — fix the backend service reference",
  "xp": 175,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "Ingress",
    "backend",
    "service reference",
    "HTTP routing"
  ],
  "module": "module-3-networking",
  "level": "level-14-ingress-defaultbackend"
}
