{
  "name": "The Mixed Signals",
  "description": "Service dual-svc exposes two ports and an Ingress routes traffic to it by port name. But the Ingress controller is rejecting the backend configuration. Run kubectl describe ingress dual-ingress -n k8smissions.",
  "objective": "A multi-port Service requires named ports — add port names so the Ingress can reference them correctly",
  "xp": 200,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "Service",
    "multi-port",
    "named ports",
    "Ingress",
    "port reference"
  ],
  "module": "module-3-networking",
  "level": "level-15-multiport-service"
}
