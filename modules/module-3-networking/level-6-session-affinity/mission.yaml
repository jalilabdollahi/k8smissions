{
  "name": "Random Logout",
  "description": "Users are randomly getting logged out. The app stores sessions in memory — each pod has its own session store. Run kubectl logs client -n k8smissions and watch which pod each request hits.",
  "objective": "The Service load-balances across pods per request — enable session affinity so clients stick to one pod",
  "xp": 200,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "Session affinity",
    "sticky sessions",
    "ClientIP",
    "stateful vs stateless apps",
    "shared session stores"
  ],
  "module": "module-3-networking",
  "level": "level-6-session-affinity"
}
