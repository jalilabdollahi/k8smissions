{
  "name": "Cross-Namespace Dead End",
  "description": "Pod frontend-app in k8smissions namespace is trying to reach an API in a different namespace. The connection always fails. Run kubectl logs frontend-app -n k8smissions.",
  "objective": "The frontend uses a short service name that doesn't resolve in another namespace — use the full <service>.<namespace>.svc.cluster.local address",
  "xp": 250,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "Cross-namespace DNS",
    "FQDN format",
    "namespace isolation"
  ],
  "module": "module-3-networking",
  "level": "level-7-cross-namespace"
}
