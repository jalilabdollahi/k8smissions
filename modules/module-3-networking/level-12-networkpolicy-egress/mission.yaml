{
  "name": "DNS Blackout",
  "description": "All pods in the namespace suddenly cannot resolve any hostname. DNS lookups time out. Applications that used to work are now broken. Run kubectl exec <any-pod> -n k8smissions -- nslookup kubernetes.",
  "objective": "A NetworkPolicy blocks all egress including DNS on port 53 — add an egress rule to allow DNS traffic",
  "xp": 225,
  "difficulty": "advanced",
  "expected_time": "15m",
  "concepts": [
    "NetworkPolicy",
    "egress",
    "DNS",
    "port 53",
    "kube-dns"
  ],
  "module": "module-3-networking",
  "level": "level-12-networkpolicy-egress"
}
