{
  "name": "NodePort Not Reachable",
  "description": "A NodePort Service web-nodeport was created for external access, but nobody knows which port to connect to — and Kubernetes may assign a different one each time. Run kubectl get service web-nodeport -n k8smissions.",
  "objective": "The nodePort value is in the reserved range below 30000 — change it to a valid nodePort number",
  "xp": 200,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "NodePort range 30000-32767",
    "external access",
    "port forwarding"
  ],
  "module": "module-3-networking",
  "level": "level-2-nodeport"
}
