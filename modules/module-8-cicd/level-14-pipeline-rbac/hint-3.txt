The `trigger-sa` ServiceAccount has no Role or RoleBinding. Tekton resources (PipelineRuns, TaskRuns) are in the `tekton.dev` API group — not the core `apps` group. A Role must explicitly grant access to this API group.