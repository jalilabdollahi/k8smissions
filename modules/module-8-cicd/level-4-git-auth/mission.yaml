{
  "name": "Clone Denied",
  "description": "A Tekton git-clone Task fails with 'authentication required'. The repository is private and the pipeline's ServiceAccount has no SSH credentials attached. Without credentials, the git-clone Task can't authenticate against the remote.",
  "objective": "The git-clone Task needs SSH credentials — bind the SSH secret to the ServiceAccount used by the TaskRun",
  "xp": 300,
  "difficulty": "intermediate",
  "expected_time": "12m",
  "concepts": [
    "Tekton",
    "git-clone",
    "secrets",
    "ServiceAccount",
    "SSH credentials"
  ],
  "module": "module-8-cicd",
  "level": "level-4-git-auth"
}
