The status should show the rollout is `Paused` at step 4. Look at the canary steps in the Rollout spec — specifically the `pause` step at 50%. A `pause: {}` with no `duration` field is an indefinite manual gate.