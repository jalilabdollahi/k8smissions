The second `pause` step has no `duration` field. Without a duration, Argo Rollouts waits indefinitely for a human to run `kubectl argo rollouts promote`. Add `duration: 5m` to make the pause time-bounded.