The TriggerBinding extracts `$(body.commits[0].sha)` but the GitHub push webhook uses `$(body.head_commit.id)` for the pushed commit's SHA. The field name mismatch means the parameter resolves to an empty string.