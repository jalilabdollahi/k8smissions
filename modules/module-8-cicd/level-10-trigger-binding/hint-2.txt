The `git-revision` param should contain a commit SHA but is empty or zero. Compare the field path in the TriggerBinding (`$(body.commits[0].sha)`) against the actual GitHub push webhook payload structure — look at the `head_commit` field.