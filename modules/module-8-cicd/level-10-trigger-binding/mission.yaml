{
  "name": "Webhook Ignored",
  "description": "Git pushes are not triggering pipeline runs. The Tekton Trigger EventListener is receiving webhook events from GitHub, but the pipeline runs it creates are failing silently — because the TriggerBinding is extracting data from the wrong field in the webhook payload.",
  "objective": "The TriggerBinding body field name doesn't match the webhook payload — fix the field reference",
  "xp": 325,
  "difficulty": "advanced",
  "expected_time": "15m",
  "concepts": [
    "Tekton Triggers",
    "TriggerBinding",
    "EventListener",
    "webhook",
    "CEL filter"
  ],
  "module": "module-8-cicd",
  "level": "level-10-trigger-binding"
}
