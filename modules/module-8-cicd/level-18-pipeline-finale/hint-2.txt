You should find three failure modes: ErrImagePull from `alpine/git:999`, a validation error about the missing `git-url` parameter value, and 403 Forbidden when the SA tries to create TaskRuns. Fix the image first — nothing else can run until pods can start.