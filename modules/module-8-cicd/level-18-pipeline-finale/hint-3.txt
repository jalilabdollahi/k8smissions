Three things need fixing: the step `image: alpine/git:999` (change to `alpine/git:latest`), the `git-url` param needs a `default` value so it's not required, and `multi-sa` needs a Role granting access to `tekton.dev` resources plus a RoleBinding.