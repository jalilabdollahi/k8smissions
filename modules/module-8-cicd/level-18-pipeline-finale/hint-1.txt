Start with a broad diagnosis: `kubectl describe taskrun -n k8smissions` and `kubectl get events -n k8smissions --sort-by=.lastTimestamp | tail -20`. Identify each distinct failure type before touching the manifest.