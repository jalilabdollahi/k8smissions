{
  "name": "Full Pipeline Down",
  "description": "The production CI/CD pipeline is completely broken. Multiple independent failures prevent any run from succeeding: a Task step uses a non-existent image tag, a required parameter has no default making the TaskRun fail validation, and the pipeline's ServiceAccount lacks RBAC to create TaskRuns. All three must be fixed before any pipeline run can complete.",
  "objective": "Fix all three pipeline issues: wrong image tag, missing workspace binding, and broken RBAC — to restore the pipeline",
  "xp": 400,
  "difficulty": "advanced",
  "expected_time": "20m",
  "concepts": [
    "Tekton",
    "pipeline debugging",
    "multi-issue",
    "image",
    "workspace",
    "RBAC"
  ],
  "module": "module-8-cicd",
  "level": "level-18-pipeline-finale"
}
