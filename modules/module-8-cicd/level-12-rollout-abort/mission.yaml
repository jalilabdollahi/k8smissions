{
  "name": "Reverted Release",
  "description": "An Argo Rollout is in Degraded state after being automatically aborted. The canary pods used a bad image tag and entered ErrImagePull — Argo Rollouts detected the failure, aborted the rollout, and rolled back traffic. The Rollout is now stuck in Degraded and won't recover until the image is fixed and the rollout is retried.",
  "objective": "The Rollout is in Degraded state after an abort — retry the rollout with a fixed image to recover",
  "xp": 300,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "Argo Rollouts",
    "abort",
    "rollback",
    "degraded state",
    "retry"
  ],
  "module": "module-8-cicd",
  "level": "level-12-rollout-abort"
}
