The `deploy-sa` ServiceAccount has no Role and no RoleBinding. The deploy Task needs permission to create and update Deployments, Services, and ConfigMaps. Both a Role granting those permissions and a RoleBinding connecting it to `deploy-sa` are missing.