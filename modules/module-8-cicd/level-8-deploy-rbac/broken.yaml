apiVersion: v1
kind: ServiceAccount
metadata:
  name: deploy-sa
  namespace: k8smissions
# ❌ BUG: ServiceAccount exists but has no Role or RoleBinding — deploy Task running as this SA gets 403 Forbidden when calling kubectl apply
