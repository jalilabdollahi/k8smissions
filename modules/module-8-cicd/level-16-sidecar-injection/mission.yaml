{
  "name": "Injected to Failure",
  "description": "Pipeline Task pods are failing to complete. Istio's admission webhook is injecting the `istio-proxy` sidecar into every pod in the namespace — including Tekton Task pods. The sidecar's lifecycle management conflicts with Tekton's own step container ordering, causing Tasks to time out or run out of sequence.",
  "objective": "The sidecar injector webhook conflicts with Tekton's step container management — disable injection for pipeline pods",
  "xp": 350,
  "difficulty": "advanced",
  "expected_time": "15m",
  "concepts": [
    "Tekton",
    "sidecar",
    "admission webhook",
    "label",
    "injection annotation"
  ],
  "module": "module-8-cicd",
  "level": "level-16-sidecar-injection"
}
