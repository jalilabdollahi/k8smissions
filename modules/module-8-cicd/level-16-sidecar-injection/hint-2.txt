You should see an `istio-proxy` container alongside the Tekton step containers. Tekton uses an init-container and entrypoint mechanism to sequence steps — the injected sidecar interferes with this mechanism.