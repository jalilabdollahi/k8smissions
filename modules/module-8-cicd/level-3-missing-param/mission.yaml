{
  "name": "Missing Parameter",
  "description": "A Tekton TaskRun fails at admission — not during execution. The Task it references requires a parameter named `IMAGE` with no default value, but the TaskRun provides an empty params list. Tekton validates required parameters before creating any pods.",
  "objective": "The Task requires a param IMAGE but no value is supplied in the TaskRun — add the required parameter",
  "xp": 250,
  "difficulty": "intermediate",
  "expected_time": "8m",
  "concepts": [
    "Tekton",
    "params",
    "TaskRun",
    "required parameters"
  ],
  "module": "module-8-cicd",
  "level": "level-3-missing-param"
}
