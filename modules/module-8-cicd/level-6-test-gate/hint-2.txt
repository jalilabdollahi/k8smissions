The logs show 'running tests' but the step exits with code 1. In Tekton (and Unix), a non-zero exit code means failure. Look at the script in the Task spec — specifically the `exit` statement at the end.