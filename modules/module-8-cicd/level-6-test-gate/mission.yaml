{
  "name": "Gate Blocks Deploy",
  "description": "The deploy stage in a CI pipeline never runs. A test Task sits before the deploy stage and acts as a gate — but the test script has `exit 1` hardcoded, which means the gate always fails regardless of whether the actual tests pass. The deploy stage is permanently blocked.",
  "objective": "The test Task has a wrong test command exit code — fix the command so successful tests return exit 0",
  "xp": 275,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "Tekton",
    "Task",
    "exit codes",
    "pipeline gates",
    "test stage"
  ],
  "module": "module-8-cicd",
  "level": "level-6-test-gate"
}
