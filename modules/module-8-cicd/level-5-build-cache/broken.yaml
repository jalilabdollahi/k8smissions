apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: build-cache-run
  namespace: k8smissions
spec:
  taskRef:
    name: maven-build
  workspaces:
  - name: source
    emptyDir: {}
  # Maven downloads all dependencies from the internet on every run — builds take 15+ minutes
