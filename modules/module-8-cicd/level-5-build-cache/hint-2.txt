You should see `source` is bound but `cache` is not. The `maven-build` Task declares both workspaces. Without a binding for `cache`, the task runs without mounting it and Maven downloads everything fresh each time.