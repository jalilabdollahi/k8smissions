The script loops indefinitely with `while [ ! -f /approval/approved ]; do sleep 5; done`. The annotation `awaiting-approval: "true"` documents the block. Change the script to `exit 0` immediately to override the gate and update the annotation to record the override reason.