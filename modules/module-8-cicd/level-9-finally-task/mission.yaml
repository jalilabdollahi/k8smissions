{
  "name": "Cleanup Skipped",
  "description": "A Pipeline runs a cleanup Task after each build to remove temporary resources. But the cleanup only runs when the build succeeds — if the build fails, the cleanup is skipped and temporary resources accumulate. The cleanup is in the wrong section of the Pipeline spec.",
  "objective": "The cleanup Task is in the tasks list, not the finally list — move it to spec.finally so it always runs",
  "xp": 275,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "Tekton",
    "finally",
    "Pipeline",
    "cleanup",
    "guaranteed execution"
  ],
  "module": "module-8-cicd",
  "level": "level-9-finally-task"
}
