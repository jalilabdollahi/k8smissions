The cleanup Task is in `spec.tasks` with `runAfter: [build]`. This means it only runs when `build` succeeds — `runAfter` is a dependency, not a guarantee. Tekton has a different section for guaranteed cleanup.