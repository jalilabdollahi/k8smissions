{
  "name": "Lost Artifact",
  "description": "A build Task produces an image digest and stores it as a Tekton result. A downstream deploy Task should receive that digest as a parameter — but the deploy Task receives an empty string every time. The result name in the pipeline's variable reference doesn't match what the producer Task declared.",
  "objective": "The Task result name in the producer doesn't match what the consumer expects — fix the result reference",
  "xp": 325,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "Tekton",
    "results",
    "inter-task data",
    "param",
    "Pipeline variables"
  ],
  "module": "module-8-cicd",
  "level": "level-17-artifact-passing"
}
