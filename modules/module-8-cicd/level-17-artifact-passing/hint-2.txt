The parameter value is empty. Look at the Pipeline spec's variable reference: `$(tasks.build.results.image-digest)`. Then check what the build Task actually names its result in `spec.results` — result names are case-sensitive.