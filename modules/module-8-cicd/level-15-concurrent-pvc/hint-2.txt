Multiple runs share `shared-build-pvc`. When two runs write to the same directory simultaneously (source checkout, build outputs), they overwrite each other's files. Each run needs its own isolated storage.