The PipelineRun uses `persistentVolumeClaim: {claimName: shared-build-pvc}` — a hardcoded shared PVC name. Replace it with `volumeClaimTemplate` so Tekton provisions a fresh, isolated PVC for each PipelineRun automatically.