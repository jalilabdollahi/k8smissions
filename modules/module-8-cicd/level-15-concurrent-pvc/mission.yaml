{
  "name": "Build Corruption",
  "description": "Concurrent pipeline runs are corrupting each other's build artifacts. Multiple PipelineRuns all reference the same hardcoded PVC name — so they all write to the same storage paths simultaneously with no locking, resulting in build failures and corrupted outputs.",
  "objective": "Multiple PipelineRuns share a single PVC without locking — add concurrency limits or per-run PVCs",
  "xp": 325,
  "difficulty": "advanced",
  "expected_time": "15m",
  "concepts": [
    "Tekton",
    "PVC",
    "concurrency",
    "workspace isolation",
    "pipeline runs"
  ],
  "module": "module-8-cicd",
  "level": "level-15-concurrent-pvc"
}
