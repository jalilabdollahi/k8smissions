The logs should show `unauthorized: authentication required`. Look at the ServiceAccount used by the pipeline: `kubectl get serviceaccount ci-sa -n k8smissions -o yaml`. Check both `secrets` and `imagePullSecrets` fields.