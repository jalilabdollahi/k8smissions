The `ci-sa` ServiceAccount has neither `secrets` nor `imagePullSecrets` attached. Tekton mounts ServiceAccount secrets into Task pods for registry authentication. Add both fields referencing the `registry-creds` Secret.