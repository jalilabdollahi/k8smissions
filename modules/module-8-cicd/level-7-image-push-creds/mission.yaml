{
  "name": "Push Denied",
  "description": "The CI pipeline's image push step fails with '401 Unauthorized' from the container registry. The pipeline ServiceAccount exists and Tasks are running, but no registry credentials are attached — so when the Task tries to push an image, the registry rejects the request.",
  "objective": "The Task is missing registry credentials — create a registry secret and bind it to the pipeline ServiceAccount",
  "xp": 300,
  "difficulty": "intermediate",
  "expected_time": "12m",
  "concepts": [
    "Tekton",
    "registry credentials",
    "docker-registry secret",
    "image push",
    "ServiceAccount"
  ],
  "module": "module-8-cicd",
  "level": "level-7-image-push-creds"
}
