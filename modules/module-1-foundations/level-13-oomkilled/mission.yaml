{
  "name": "Out of Memory",
  "description": "Pod oom-pod keeps restarting. Run kubectl get pod oom-pod -n k8smissions and note the STATUS and RESTARTS values.",
  "objective": "The memory limit is set far too low for the application — increase it so the container isn't killed by the OOM killer",
  "xp": 150,
  "difficulty": "beginner",
  "expected_time": "10m",
  "concepts": [
    "OOMKilled",
    "memory limits",
    "exit code 137",
    "resource tuning"
  ],
  "module": "module-1-foundations",
  "level": "level-13-oomkilled"
}
