{
  "name": "The Everywhere Pod",
  "description": "DaemonSet log-collector should run one pod on every node, but its pods are stuck in Pending. Run kubectl get pods -n k8smissions -o wide to see which nodes they were assigned to.",
  "objective": "The DaemonSet has a missing toleration — add the correct toleration so pods schedule on all nodes including control-plane",
  "xp": 150,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "DaemonSet",
    "tolerations",
    "taints",
    "control-plane"
  ],
  "module": "module-1-foundations",
  "level": "level-16-daemonset"
}
