{
  "name": "The Infinite Restart",
  "description": "Pod crashloop-pod is stuck in CrashLoopBackOff. Run kubectl get pods -n k8smissions and watch the RESTARTS column climb.",
  "objective": "The container args reference a script that doesn't exist — fix the startup args so the container runs without immediately exiting",
  "xp": 100,
  "difficulty": "beginner",
  "expected_time": "8m",
  "concepts": [
    "CrashLoopBackOff",
    "args",
    "restartPolicy",
    "exit codes"
  ],
  "module": "module-1-foundations",
  "level": "level-2-crashloop"
}
