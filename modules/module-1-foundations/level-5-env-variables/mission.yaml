{
  "name": "Missing the Key",
  "description": "Pod env-pod starts but crashes almost immediately. Run kubectl logs env-pod -n k8smissions and read what the application says.",
  "objective": "The application requires an environment variable that is missing from the pod spec — add the missing env var",
  "xp": 150,
  "difficulty": "beginner",
  "expected_time": "10m",
  "concepts": [
    "env",
    "environment variables",
    "application config"
  ],
  "module": "module-1-foundations",
  "level": "level-5-env-variables"
}
