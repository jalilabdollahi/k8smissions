The Secret `db-credentials` does not exist. The pod spec has two env vars that use `secretKeyRef` to pull values from it.

You need to create this Secret and add it to manifest.yaml — above the Pod definition.