{
  "name": "Silent Failure",
  "description": "Pod database-app starts and exits repeatedly. The logs are the only clue. Run kubectl logs database-app -n k8smissions and read the full output.",
  "objective": "Use the container logs to identify the required PostgreSQL setting, then add the missing environment variable",
  "xp": 150,
  "difficulty": "beginner",
  "expected_time": "12m",
  "concepts": [
    "kubectl logs",
    "application logs",
    "debugging startup failures",
    "environment variables"
  ],
  "module": "module-1-foundations",
  "level": "level-14-log-debugging"
}
