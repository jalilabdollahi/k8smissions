{
  "name": "Silent Failure",
  "description": "A PostgreSQL Pod repeatedly exits during startup and never becomes Ready.",
  "objective": "Use the container logs to identify the required PostgreSQL setting, then add the missing environment variable",
  "xp": 150,
  "difficulty": "beginner",
  "expected_time": "12m",
  "concepts": [
    "kubectl logs",
    "application logs",
    "debugging startup failures",
    "environment variables"
  ],
  "module": "module-1-foundations",
  "level": "level-14-log-debugging"
}
