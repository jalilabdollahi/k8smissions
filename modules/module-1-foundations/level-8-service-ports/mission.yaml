{
  "name": "Connection Refused",
  "description": "Pod web-server is Running and the Service has endpoints. But connecting to the Service returns 'Connection refused'. Run kubectl get endpoints web-service -n k8smissions to confirm endpoints exist.",
  "objective": "The service targetPort points to the wrong container port — correct it to match what the container actually listens on",
  "xp": 100,
  "difficulty": "beginner",
  "expected_time": "8m",
  "concepts": [
    "port",
    "targetPort",
    "containerPort",
    "service routing"
  ],
  "module": "module-1-foundations",
  "level": "level-8-service-ports"
}
