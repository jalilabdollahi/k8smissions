{
  "name": "Config Not Found",
  "description": "Pod configmap-pod is stuck in ContainerCreating and never reaches Running. Run kubectl describe pod configmap-pod -n k8smissions to find out why.",
  "objective": "The pod mounts a ConfigMap that doesn't exist yet — create the missing ConfigMap so the pod can start",
  "xp": 150,
  "difficulty": "beginner",
  "expected_time": "10m",
  "concepts": [
    "ConfigMap",
    "volumes",
    "volumeMounts",
    "envFrom"
  ],
  "module": "module-1-foundations",
  "level": "level-9-configmap-basics"
}
