{
  "name": "The Undeletable Object",
  "description": "ConfigMap stuck-config is in Terminating state and has been for several minutes. Run kubectl get configmap stuck-config -n k8smissions.",
  "objective": "A custom finalizer was never cleared — remove the finalizer so the object can be garbage collected",
  "xp": 200,
  "difficulty": "intermediate",
  "expected_time": "12m",
  "concepts": [
    "finalizers",
    "garbage collection",
    "kubectl patch",
    "object lifecycle"
  ],
  "module": "module-1-foundations",
  "level": "level-20-finalizer-stuck"
}
