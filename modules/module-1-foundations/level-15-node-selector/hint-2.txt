The scheduler says no node matches the nodeSelector. Check what labels your nodes actually have:

  kubectl get nodes --show-labels

Look through the labels carefully. Is `mission=moon-base` present on any node?