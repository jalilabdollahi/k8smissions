A pod with no explicit memory limit should not be OOMKilled unless something else is injecting a limit. Check if the namespace has a LimitRange:

  kubectl get limitrange -n k8smissions

Does anything show up?