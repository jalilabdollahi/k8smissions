{
  "name": "The Noisy Neighbour",
  "description": "A memory-hungry Pod keeps restarting with OOMKilled even though its manifest does not declare a memory limit.",
  "objective": "Find the namespace LimitRange that injected a small default limit, then give the Pod an explicit memory limit large enough for the workload",
  "xp": 175,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "LimitRange",
    "resource limits",
    "resource requests",
    "namespace policy",
    "admission defaults"
  ],
  "module": "module-1-foundations",
  "level": "level-19-limitrange"
}
