{
  "name": "One-Shot Task",
  "description": "Job db-init has reached BackoffLimitExceeded. Run kubectl get jobs -n k8smissions to see its current state.",
  "objective": "The Job container command exits non-zero — fix the command so the Job completes successfully",
  "xp": 150,
  "difficulty": "intermediate",
  "expected_time": "8m",
  "concepts": [
    "Job",
    "backoffLimit",
    "exit codes",
    "completions",
    "batch"
  ],
  "module": "module-1-foundations",
  "level": "level-17-job-completion"
}
