The command is `/scripts/init-db.sh`. This script does not exist inside the busybox:1.36 image — it exits with code 127 (command not found).

Look at spec.template.spec.containers[0].command in manifest.yaml.