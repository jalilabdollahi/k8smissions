{
  "name": "Waiting Forever",
  "description": "Pod web-with-init is stuck in Init:0/1 and never reaches Running. Run kubectl get pod web-with-init -n k8smissions.",
  "objective": "The init container waits for a service that doesn't exist and blocks forever — fix the dependency so the init container can complete",
  "xp": 200,
  "difficulty": "intermediate",
  "expected_time": "15m",
  "concepts": [
    "initContainers",
    "pod initialization",
    "startup ordering",
    "dependency waiting"
  ],
  "module": "module-1-foundations",
  "level": "level-12-init-container"
}
