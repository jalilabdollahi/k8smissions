Read the init container's output and inspect its command:
  kubectl logs web-with-init -c wait-for-service -n k8smissions
  kubectl get pod web-with-init -n k8smissions -o yaml
It repeatedly looks up backend-service, which does not exist.
