{
  "name": "Sidecar Sabotage",
  "description": "Pod app-with-logging has two containers. Run kubectl get pod app-with-logging -n k8smissions and look at the READY column. What does it show?",
  "objective": "The sidecar container has a bad command that causes it to crash immediately — fix the sidecar command so both containers run",
  "xp": 200,
  "difficulty": "intermediate",
  "expected_time": "15m",
  "concepts": [
    "multi-container pods",
    "sidecar pattern",
    "shared volumes",
    "container independence"
  ],
  "module": "module-1-foundations",
  "level": "level-11-multi-container"
}
