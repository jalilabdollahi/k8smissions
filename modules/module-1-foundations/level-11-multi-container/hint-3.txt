The sidecar runs `tail -f /var/log/nonexistent.log`. The file does not exist in the shared emptyDir volume — `tail` exits with an error immediately.

The fix is in spec.containers[1].command in manifest.yaml.