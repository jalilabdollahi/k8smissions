{
  "name": "Lost in Space",
  "description": "Resources were created but the validator cannot find them in the k8smissions namespace. Start with kubectl get pods -n k8smissions — is anything there?",
  "objective": "The pod and service are in the wrong namespace — move both to the k8smissions namespace where the app expects to run",
  "xp": 150,
  "difficulty": "beginner",
  "expected_time": "10m",
  "concepts": [
    "namespaces",
    "resource isolation",
    "cross-namespace DNS"
  ],
  "module": "module-1-foundations",
  "level": "level-6-namespace-mismatch"
}
