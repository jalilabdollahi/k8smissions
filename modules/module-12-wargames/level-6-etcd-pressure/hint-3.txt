Delete all `ci-run-*` ConfigMaps: `kubectl delete configmap -n k8smissions $(kubectl get cm -n k8smissions --no-headers | awk '/^ci-run-/{print $1}')`. Then prevent future leaks by adding `ttlSecondsAfterFinished: 300` to CI Job specs.