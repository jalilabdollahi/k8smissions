The quota shows pods Used is near or at its Hard limit. The 20 `ci-test-pod-*` pods are in `Completed` state but still count against the quota. Completed pods don't free quota slots — only deletion does.