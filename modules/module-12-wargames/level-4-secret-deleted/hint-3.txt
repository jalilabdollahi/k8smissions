Recreate the Secret with the correct key names: `username` and `password` (matching what `secretKeyRef.key` expects). Once the Secret exists, Kubernetes will automatically retry the pod creation and it will transition to Running.