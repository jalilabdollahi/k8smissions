`kubectl auth can-i` should return 'no'. You should see `app-sa` (ServiceAccount) and `app-role` (Role) in the output, but NO RoleBinding. Without the RoleBinding, the ServiceAccount has exactly zero permissions despite the Role existing.