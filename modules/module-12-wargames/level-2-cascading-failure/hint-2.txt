You should see `auth-svc` with 0/0 pods. Check that it has no endpoints: `kubectl get endpoints auth-svc -n k8smissions`. The Service exists but has empty endpoints — all callers receive 'connection refused' on every auth call.