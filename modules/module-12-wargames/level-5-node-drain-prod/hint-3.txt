The PDB has `minAvailable: 3` but the Deployment has `replicas: 2`. Change `minAvailable` to `1` so that 1 pod can be evicted (draining) while 1 pod remains available: `allowed = 2 - 1 = 1` disruption.