Look for OOMKilled events in the output — these indicate the container was killed by the kernel for exceeding its memory limit. Then check the current memory limit on the Deployment: `kubectl describe deployment incident-app -n k8smissions | grep -A5 Limits`