{
  "name": "The Slow Starter",
  "description": "An application pod keeps restarting during container startup. The app takes 120 seconds to complete initialization before it signals readiness, but the liveness probe is configured with timing that kills the container after only 40 seconds — long before initialization can finish.",
  "objective": "The liveness probe fires before the app finishes starting — add a startupProbe to give the app time to boot",
  "xp": 200,
  "difficulty": "intermediate",
  "expected_time": "12m",
  "concepts": [
    "startupProbe",
    "liveness probe",
    "slow start",
    "initialDelaySeconds"
  ],
  "module": "module-6-observability",
  "level": "level-13-startup-probe"
}
