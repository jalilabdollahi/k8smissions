Calculate the total time before the liveness probe kills the pod: `initialDelaySeconds + (failureThreshold × periodSeconds)`. Compare that number to how long the app actually needs to boot (look at the command — it sleeps for 120 seconds before creating `/tmp/ready`).