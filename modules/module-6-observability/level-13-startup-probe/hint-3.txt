The pod has no `startupProbe`. Without one, the liveness probe begins after `initialDelaySeconds: 10` and kills the container after 3 × 10s = 30 more seconds — at 40s total. The app needs 120s to boot, so it is always killed 80 seconds too early.