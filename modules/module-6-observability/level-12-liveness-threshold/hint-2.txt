Note the `failureThreshold` value. With `failureThreshold: 1`, a single missed probe check — caused by any momentary system load or scheduling delay — immediately triggers a container restart. The Kubernetes default is 3.