{
  "name": "Restart Storm",
  "description": "A pod's RESTARTS counter keeps climbing every few minutes even though the application is working correctly and the logs show no errors. The restarts are being triggered by the health check configuration — a single transient delay is enough to cause a restart.",
  "objective": "The liveness probe has a failureThreshold of 1, causing restarts on any transient delay — increase the threshold",
  "xp": 175,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "liveness probe",
    "failureThreshold",
    "successThreshold",
    "probe tuning",
    "CrashLoopBackOff"
  ],
  "module": "module-6-observability",
  "level": "level-12-liveness-threshold"
}
