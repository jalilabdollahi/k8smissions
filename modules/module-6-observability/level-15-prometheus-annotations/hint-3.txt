The pod's `metadata.annotations` block is missing entirely. Three annotations are needed: `prometheus.io/scrape` to enable discovery, `prometheus.io/port` to specify which port to scrape, and `prometheus.io/path` to specify the metrics endpoint path.