There are no annotations at all. Prometheus uses specific pod annotations to discover which pods to scrape. Without `prometheus.io/scrape: 'true'`, Prometheus ignores this pod entirely regardless of whether the metrics endpoint works.