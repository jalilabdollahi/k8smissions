{
  "name": "Invisible Metrics",
  "description": "A service is emitting Prometheus metrics on port 9090 but they never appear in monitoring dashboards. The pod is Running, the port is exposed, and the metrics endpoint is working — but Prometheus has no way to discover this pod because the annotation-based discovery configuration is entirely missing.",
  "objective": "The pod is missing Prometheus scrape annotations — add the required annotations to enable metrics collection",
  "xp": 200,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "Prometheus",
    "annotations",
    "scraping",
    "metrics",
    "observability"
  ],
  "module": "module-6-observability",
  "level": "level-15-prometheus-annotations"
}
