The config file should show `DB_HOST` set to a raw IP address like `10.0.0.99`. In Kubernetes, pod IP addresses are ephemeral — they change every time a pod restarts. Services should always be referenced by their DNS name.