{
  "name": "Diagnose the Live Pod",
  "description": "A pod is Running and passing health checks, but the application returns 500 errors with no useful log output. The problem is in the configuration file the app loaded at startup — not in the code itself. You need to exec into the live container and read what configuration it actually has.",
  "objective": "The app returns 500 errors with no logs — exec into the container and inspect its config files to find the misconfiguration",
  "xp": 300,
  "difficulty": "advanced",
  "expected_time": "10m",
  "concepts": [
    "kubectl exec",
    "ephemeral containers (kubectl debug)",
    "live debugging",
    "configuration inspection"
  ],
  "module": "module-6-observability",
  "level": "level-9-exec-debugging"
}
