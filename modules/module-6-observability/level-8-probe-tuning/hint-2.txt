The events show the liveness probe failing during startup. Look at `initialDelaySeconds` — that is when the first probe fires. If the app takes longer than that to start, every startup attempt will be killed by the liveness probe.