You should see `LOG_LEVEL: DEBUG` in the environment variables. DEBUG logging emits every internal state change and is appropriate only during active development. Look at the manifest's `env` block to find where to change this.