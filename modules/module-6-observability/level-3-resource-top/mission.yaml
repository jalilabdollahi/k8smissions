{
  "name": "Who's Eating All the CPU?",
  "description": "The cluster node is experiencing CPU pressure and general performance is degraded. A pod is consuming far more CPU than it declared in its requests. You need to find it using metrics and then cap its consumption by adding a CPU limit.",
  "objective": "A pod is consuming excess CPU and impacting the node — use kubectl top to find and limit it",
  "xp": 250,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "kubectl top",
    "resource limits vs requests",
    "noisy-neighbor",
    "LimitRange to enforce defaults"
  ],
  "module": "module-6-observability",
  "level": "level-3-resource-top"
}
