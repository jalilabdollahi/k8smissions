{
  "name": "Requests Dropped During Rollout",
  "description": "Users experience connection reset errors during rolling updates. Pods are being killed while they still have active connections. There is nothing telling Kubernetes to wait before sending SIGTERM or to give the application time to drain — so in-flight requests are dropped the moment the pod is removed from the endpoints list.",
  "objective": "Pods are terminated mid-request during rollouts — add a preStop hook and set terminationGracePeriodSeconds",
  "xp": 350,
  "difficulty": "advanced",
  "expected_time": "10m",
  "concepts": [
    "Pod termination lifecycle (SIGTERM → grace period → SIGKILL)",
    "preStop hooks",
    "connection draining",
    "zero-downtime deploys"
  ],
  "module": "module-7-gitops",
  "level": "level-6-graceful-shutdown"
}
