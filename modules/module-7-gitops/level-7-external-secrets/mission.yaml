{
  "name": "Secrets in Git? Never.",
  "description": "A security audit has flagged plain-text credentials committed directly to the Git repository as a Kubernetes Secret manifest. Kubernetes Secrets are only base64-encoded, not encrypted — anyone with repository access can decode them. The fix is to delegate secret management to the External Secrets Operator.",
  "objective": "Plain-text credentials are committed in Git — replace the inline Secret with an ExternalSecret managed by ESO",
  "xp": 500,
  "difficulty": "expert",
  "expected_time": "10m",
  "concepts": [
    "External Secrets Operator",
    "SecretStore",
    "ExternalSecret",
    "Vault integration",
    "Sealed Secrets alternative",
    "secret rotation"
  ],
  "module": "module-7-gitops",
  "level": "level-7-external-secrets"
}
