Plain-text credentials in a Git-committed manifest is a critical security finding. The industry standard is to remove the credentials from Git entirely and have a secret management system (like Vault or AWS Secrets Manager) inject them at runtime via the External Secrets Operator.