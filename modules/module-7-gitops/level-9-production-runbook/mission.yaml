{
  "name": "Runbook: DB Migration Rollout",
  "description": "A database migration must run before the application can serve traffic safely. The migration Job is failing immediately due to two separate problems: the container image tag doesn't exist and the command always exits with an error code. Both must be fixed before the migration can succeed.",
  "objective": "Run the database migration safely: scale down the app, apply the migration job, verify it completes, then scale back up",
  "xp": 450,
  "difficulty": "expert",
  "expected_time": "10m",
  "concepts": [
    "Kubernetes Jobs",
    "initContainers for DB migration",
    "runbook execution",
    "ordered operations",
    "Job completions/backoffLimit"
  ],
  "module": "module-7-gitops",
  "level": "level-9-production-runbook"
}
