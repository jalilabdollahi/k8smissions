The Job has two broken fields: `image: busybox:missing` (a nonexistent tag) and `command: ['/bin/sh', '-c', 'exit 1']` (which always fails). Fix both — use `busybox:1.36` and change the command to something that exits 0.