The patch targets a Deployment named `backend-api` but the actual Deployment is named `api-server`. Kustomize found no resource matching the patch target and silently skipped the patch — no error is reported.