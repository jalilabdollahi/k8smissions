The replica count is still at the base value (1), not the patched value (5). Compare the resource name in the patch `target.name` against the actual name of the Deployment in the cluster — they must match exactly.