Run a full production readiness review first: `kubectl describe deployment grand-finale-app -n k8smissions` and `kubectl describe pdb grand-finale-pdb -n k8smissions`. List every issue you find before touching the manifest.