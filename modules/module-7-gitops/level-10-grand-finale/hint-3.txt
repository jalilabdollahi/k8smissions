Six things need fixing across two resources: container `image` tag, `spec.replicas`, `resources.limits`, `readinessProbe`, `lifecycle.preStop` + `terminationGracePeriodSeconds` on the Deployment, and `spec.minAvailable` on the PDB.