You should find `finalizers: [some.controller.io/cleanup]` on the ConfigMap. This finalizer was registered by a controller that has since been deleted. Without the controller running, the finalizer will never be removed — it must be force-cleared manually.