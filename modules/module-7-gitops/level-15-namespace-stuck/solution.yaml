apiVersion: v1
kind: Namespace
metadata:
  name: zombie-ns
  finalizers: []          # ✅ FIX: namespace finalizer cleared — namespace can now complete deletion
status:
  phase: Terminating
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: stuck-resource
  namespace: zombie-ns
  finalizers: []          # ✅ FIX: object finalizer removed via kubectl patch — no operator to clear it, so we force-clear it
data:
  key: value
