The ConfigMap `stuck-resource` has a finalizer `some.controller.io/cleanup` that will never be cleared automatically. You need to patch the ConfigMap to set `finalizers: []`, which removes the block and allows the namespace deletion to complete.