You should see `image: nginx:latest`. In a Helm chart, this comes from `{{ .Values.image.tag }}` in the template. When the tag is empty or unset, the chart falls back to `latest`. Look at the manifest's `image` field — it needs a pinned version.