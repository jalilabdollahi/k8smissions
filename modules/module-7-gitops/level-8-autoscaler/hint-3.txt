The Cluster Autoscaler's `--node-group-auto-discovery` argument has `asg:tag=wrong-group` but the actual node group uses the tag `kind-worker`. The tag mismatch means the autoscaler discovers zero node groups and cannot add any nodes.