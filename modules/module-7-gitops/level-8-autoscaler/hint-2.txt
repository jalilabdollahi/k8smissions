The logs should show the autoscaler searching for node groups tagged with `wrong-group` and finding none. Now look at the autoscaler Deployment's `args` to see the full `--node-group-auto-discovery` value.