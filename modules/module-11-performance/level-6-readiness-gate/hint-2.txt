The pod reaches Running state and Kubernetes marks it Ready because there's no readinessProbe defined. Without a probe, Kubernetes assumes the pod is ready the moment the container starts — but a Java application can take 30+ seconds to fully initialize.