Watch pod status during a rollout: `kubectl rollout status deployment/java-app -n k8smissions`. Notice pods transition to Ready very quickly, but check if they actually respond: `kubectl exec <pod> -- curl -f http://localhost:8080/health/ready`