Count DNS queries per second on a pod: `kubectl exec dns-heavy-app -n k8smissions -- cat /etc/resolv.conf`. Look at the `ndots` and `search` values — each short lookup triggers a search through each of those domains.