{
  "name": "DNS Flood",
  "description": "A high-throughput application makes hundreds of DNS lookups per second using short names like `db`, `cache`, `api`. These lookups are generating massive load on kube-dns and have high latency. The default `ndots: 5` setting causes each short name to be searched across 5 DNS suffixes before resolving, multiplying DNS traffic by 5x.",
  "objective": "The app uses short hostnames triggering FQDN search list expansion — set ndots: 2 to reduce unnecessary DNS lookups",
  "xp": 325,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "DNS",
    "ndots",
    "resolv.conf",
    "kube-dns",
    "DNS performance"
  ],
  "module": "module-11-performance",
  "level": "level-3-dns-ndots"
}
