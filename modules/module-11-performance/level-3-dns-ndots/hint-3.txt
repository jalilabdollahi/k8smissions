Add `dnsConfig.options` with `ndots: 2` to reduce the search suffix attempts. Also change the DNS lookup in the container command to use the full FQDN `db.k8smissions.svc.cluster.local` instead of `db`.