The `terminationGracePeriodSeconds: 5` gives pods only 5 seconds to finish requests before SIGKILL. Long-running requests (>5s) are killed mid-flight. There's also a race: when a pod gets SIGTERM, the load balancer may still route traffic to it for several seconds while endpoint updates propagate.