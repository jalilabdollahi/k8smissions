Increase `terminationGracePeriodSeconds` to `60`. Add a `lifecycle.preStop.exec` hook that runs `sleep 10` — this delays SIGTERM for 10 seconds, giving the load balancer time to stop routing traffic to the pod before the app starts shutting down.