The `timeout: 15s` is too short for operations that take up to 120 seconds. Increase it to `timeout: 120s` and add a `retries` block with `attempts: 3`, `perTryTimeout: 30s`, and `retryOn: 5xx,reset,connect-failure` to handle transient failures.