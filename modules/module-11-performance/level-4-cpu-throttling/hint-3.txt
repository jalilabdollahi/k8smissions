The CPU limit equals the CPU request — there's no headroom for bursting. Increase `limits.cpu` to `1000m` (10x the request) to allow the container to use available CPU capacity during spikes without being throttled.