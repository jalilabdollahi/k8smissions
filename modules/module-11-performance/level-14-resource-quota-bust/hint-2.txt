You should see the `Used` values at or near the `Hard` limits. The quota has `requests.cpu: 4` and `requests.memory: 8Gi` but the sum of all pod requests exceeds these values. Any new pod or scale-up is rejected.