The ResourceQuota has `requests.cpu: 4` which is too low for the current workload. Increase it to `requests.cpu: 8` and `requests.memory: 16Gi` (and the corresponding limits) to give the namespace room to grow.