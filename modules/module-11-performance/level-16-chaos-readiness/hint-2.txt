Both pods may be on the same node — or at best on two out of many available nodes. With only 2 replicas, one node failure eliminates at least half the capacity. The deployment has no constraints forcing pods to spread across nodes.