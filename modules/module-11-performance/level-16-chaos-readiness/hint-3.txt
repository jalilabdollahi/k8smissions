Increase `replicas` from 2 to 5. Add `topologySpreadConstraints` with `topologyKey: kubernetes.io/hostname` and `maxSkew: 1` to force pods to spread evenly across nodes — so no single node failure can take down more than ~20% of capacity.