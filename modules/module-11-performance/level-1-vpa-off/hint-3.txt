The VPA has `updatePolicy.updateMode: Off`. This means VPA only observes and reports — it never acts. Change it to `updateMode: Auto` so VPA will evict and restart pods with right-sized resource requests.