{
  "name": "OOM Killed Daily",
  "description": "A VerticalPodAutoscaler exists for the deployment, but pods keep getting OOMKilled. The VPA is collecting usage data and generating recommendations — but it's configured in `Off` mode, meaning it never actually applies those recommendations. Pods run with their original resource requests forever.",
  "objective": "Enable Vertical Pod Autoscaler (VPA) in Recommendation mode to automatically determine correct resource requests/limits",
  "xp": 300,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "VPA",
    "VerticalPodAutoscaler",
    "OOMKilled",
    "resource tuning",
    "recommendations"
  ],
  "module": "module-11-performance",
  "level": "level-1-vpa-off"
}
