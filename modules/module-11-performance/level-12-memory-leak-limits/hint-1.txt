Watch the memory usage of the leaky pod over time: `kubectl top pod -n k8smissions -l app=leaky`. Compare the memory shown against any limits: `kubectl get deployment leaky-app -n k8smissions -o yaml | grep -A8 resources`