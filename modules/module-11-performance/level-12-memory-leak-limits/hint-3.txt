The `leaky-app` container has no `limits` defined. Add `limits.memory: 512Mi` to cap the container's memory usage. When it hits the limit, the OOM killer restarts only this container, not neighboring pods.