The pod shows growing memory usage and the `resources` section only has `requests` — there are no `limits`. Without a memory limit, the container can consume all available memory on the node. When node memory is exhausted, the OOM killer terminates processes — often killing neighboring pods.