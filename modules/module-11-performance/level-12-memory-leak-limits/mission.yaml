{
  "name": "Slow Memory Leak",
  "description": "A deployment is gradually consuming more and more memory over time. Eventually the pod's memory usage exceeds the node's available memory and the Linux OOM killer starts killing other pods on the same node — including unrelated, healthy workloads. The leaky pod has no memory limit set, so it grows without bound.",
  "objective": "The pod has no memory limit — setting a limit causes OOMKill at a predictable threshold, giving time to fix the leak",
  "xp": 325,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "memory leak",
    "OOMKilled",
    "memory limits",
    "resource management",
    "monitoring"
  ],
  "module": "module-11-performance",
  "level": "level-12-memory-leak-limits"
}
