The strategy has `maxSurge: 0` and `maxUnavailable: 1`. Change to `maxSurge: 10` and `maxUnavailable: 0` to replace 10 pods in parallel while keeping all original pods running until replacements are ready.