Check the rolling update strategy: `kubectl get deployment slow-deploy -n k8smissions -o yaml | grep -A5 rollingUpdate`. With `maxSurge: 0` and `maxUnavailable: 1`, the deployment creates no extra capacity and removes/replaces exactly 1 pod at a time.