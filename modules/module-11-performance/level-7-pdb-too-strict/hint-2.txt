The PDB shows `minAvailable: 3` and the deployment has `replicas: 3`. With 3 pods required minimum and 3 total, evicting any single pod would drop below 3 — so the PDB blocks all evictions. `kubectl describe pdb my-pdb -n k8smissions` should show `ALLOWED DISRUPTIONS: 0`.