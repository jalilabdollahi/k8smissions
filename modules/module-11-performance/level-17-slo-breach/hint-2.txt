The liveness probe has `periodSeconds: 10` and `failureThreshold: 10`, meaning a crashed container can fail 10 consecutive checks before being restarted — that's 100 seconds of downtime per crash. The annotation shows a 99.9% SLO with only 43.8 minutes/month of error budget.