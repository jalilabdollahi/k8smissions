The `failureThreshold: 10` allows 100 seconds (10 failures × 10 seconds) of downtime before restart. Reduce it to `3` to trigger a restart after just 30 seconds of failures, significantly reducing per-incident downtime.