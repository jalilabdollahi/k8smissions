Start with observation: `kubectl top pods -n k8smissions` for CPU usage, `kubectl rollout status deployment/slow-rollout -n k8smissions` for rollout progress, and `kubectl exec dns-pod -n k8smissions -- cat /etc/resolv.conf` for DNS config. Identify all three problems before touching anything.