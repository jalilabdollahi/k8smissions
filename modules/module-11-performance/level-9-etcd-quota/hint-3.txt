The `quota-backend-bytes` is set to 2GB (`2147483648`). Increase it to 8GB (`8589934592`) — the recommended production value. Also verify `auto-compaction-retention: 1h` is set to prevent accumulation of old revisions.