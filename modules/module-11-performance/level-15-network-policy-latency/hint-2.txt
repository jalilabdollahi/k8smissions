In large clusters, iptables-based NetworkPolicy creates thousands of rules.
For every packet, the kernel evaluates all rules sequentially — O(n) per packet.
With only 1000 pods, iptables may have 10,000+ rules per node.
