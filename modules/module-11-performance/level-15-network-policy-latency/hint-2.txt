With iptables-based NetworkPolicy enforcement (e.g., kube-proxy iptables mode), each new NetworkPolicy rule adds iptables chain entries that are traversed linearly. With hundreds of policies, every packet traverses hundreds of iptables rules — adding measurable latency.