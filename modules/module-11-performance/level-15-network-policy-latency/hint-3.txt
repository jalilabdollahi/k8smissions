Add Cilium-specific annotations to the NetworkPolicy: `policy-engine: cilium-ebpf` and `optimization: O(1)-lookup-enabled`. These annotations signal to Cilium to use its eBPF-based policy enforcement, which uses hash-map lookups instead of linear iptables chain traversal.