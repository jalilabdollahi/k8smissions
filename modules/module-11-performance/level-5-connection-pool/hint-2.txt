With 10 replicas and `DB_POOL_SIZE: 1`, the app has a maximum of 10 simultaneous database queries across the entire deployment. Any traffic spike beyond 10 concurrent DB operations causes requests to queue. No `DB_POOL_TIMEOUT` means queued requests wait indefinitely.