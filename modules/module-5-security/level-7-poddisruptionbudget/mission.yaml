{
  "name": "PDB Blocks Node Drain",
  "description": "A routine node maintenance procedure is failing. The operator runs `kubectl drain` but it errors immediately saying pod eviction would violate the disruption budget. The Deployment is running but its replica count and the PDB's minimum availability requirement are mathematically incompatible.",
  "objective": "The PDB's minAvailable is higher than the Deployment replica count — lower minAvailable so drain can proceed",
  "xp": 300,
  "difficulty": "advanced",
  "expected_time": "10m",
  "concepts": [
    "PodDisruptionBudget",
    "voluntary disruption",
    "cluster upgrades",
    "kubectl drain --ignore-daemonsets"
  ],
  "module": "module-5-security",
  "level": "level-7-poddisruptionbudget"
}
