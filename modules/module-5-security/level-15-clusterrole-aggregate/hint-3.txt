The `deployments-reader` ClusterRole has the label `rbac.example.io/aggregate-to-viewer: 'true'`, but `operator-role`'s aggregationRule selects for `rbac.example.io/aggregate-to-operator: 'true'`. The label value string is wrong — it says `viewer` instead of `operator`.