The `rules:` section is empty despite the source ClusterRole existing. Compare what the `aggregationRule.clusterRoleSelectors` requires against the label on `deployments-reader`: `kubectl get clusterrole deployments-reader -o yaml | grep -A5 labels`