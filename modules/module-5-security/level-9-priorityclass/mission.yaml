{
  "name": "Critical Pods Can't Schedule",
  "description": "A critical API pod is stuck in Pending even though lower-priority development workloads are consuming the available CPU on the node. The scheduler cannot evict the dev pods to make room because there is no priority difference between them — no PriorityClass has been defined or assigned.",
  "objective": "The critical pod has no PriorityClass — assign it a high-priority class so it can preempt lower-priority workloads",
  "xp": 300,
  "difficulty": "advanced",
  "expected_time": "10m",
  "concepts": [
    "PriorityClass",
    "preemption",
    "pod priority values",
    "PriorityClass.globalDefault",
    "scheduling order"
  ],
  "module": "module-5-security",
  "level": "level-9-priorityclass"
}
