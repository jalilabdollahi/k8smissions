Start with a broad scan before fixing anything: `kubectl get all -n k8smissions` and `kubectl get events -n k8smissions --sort-by=.lastTimestamp | tail -20`. List every distinct failure type you observe before touching the manifest.