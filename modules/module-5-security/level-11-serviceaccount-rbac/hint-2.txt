The logs should show 'FORBIDDEN' after `kubectl get configmaps`. Verify what RBAC is attached to the ServiceAccount: `kubectl get role,rolebinding -n k8smissions`. You should find nothing related to config-reader-sa.