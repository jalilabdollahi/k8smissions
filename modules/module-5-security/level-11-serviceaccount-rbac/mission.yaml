{
  "name": "Forbidden Access",
  "description": "A pod that reads ConfigMaps at startup to load application configuration is printing FORBIDDEN and exiting. The ServiceAccount it uses was created and assigned, but it was deployed without any RBAC permissions — making every API call it attempts a 403 error.",
  "objective": "The pod's ServiceAccount has no Role/RoleBinding — create the RBAC resources to grant configmap read access",
  "xp": 200,
  "difficulty": "intermediate",
  "expected_time": "12m",
  "concepts": [
    "ServiceAccount",
    "Role",
    "RoleBinding",
    "RBAC",
    "API access"
  ],
  "module": "module-5-security",
  "level": "level-11-serviceaccount-rbac"
}
