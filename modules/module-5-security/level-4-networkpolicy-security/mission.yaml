{
  "name": "Locked Down Too Hard",
  "description": "After the security team applied a new NetworkPolicy, the backend service stopped reaching the database. Both pods are running, the Service exists, and DNS resolves correctly — but every connection attempt times out. The policy that was applied is too broad.",
  "objective": "A default-deny NetworkPolicy blocks all traffic — add targeted allow rules to restore the required service connections",
  "xp": 350,
  "difficulty": "advanced",
  "expected_time": "10m",
  "concepts": [
    "NetworkPolicy default-deny",
    "ingress/egress",
    "podSelector",
    "namespaceSelector",
    "port-specific rules",
    "defense in depth"
  ],
  "module": "module-5-security",
  "level": "level-4-networkpolicy-security"
}
