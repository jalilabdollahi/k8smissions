List the NetworkPolicies and read their specs: `kubectl get networkpolicy -n k8smissions -o yaml`. Look for a policy that selects all pods (`podSelector: {}`) but has `policyTypes: [Ingress, Egress]` with no actual rule blocks.