Apply the manifest and read the admission error directly: `kubectl apply -f ~/.k8smissions/sandbox/manifest.yaml`. If already applied, check events: `kubectl get events -n k8smissions --sort-by=.lastTimestamp | tail -5`