The quota output will show `requests.cpu` near its hard limit. Now check which pod is responsible: `kubectl describe pod quota-buster -n k8smissions` and look at the `resources.requests` section in the spec.