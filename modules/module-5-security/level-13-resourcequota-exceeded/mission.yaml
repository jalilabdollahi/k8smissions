{
  "name": "Quota Breached",
  "description": "New pods in this namespace fail to schedule with 'exceeded quota' errors even though the node has plenty of free CPU. The problem is not node capacity — one existing pod is consuming nearly all of the namespace's CPU quota allocation, leaving almost nothing for other workloads.",
  "objective": "Namespace ResourceQuota limits are exceeded — reduce resource requests or remove the over-requesting pod",
  "xp": 175,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "ResourceQuota",
    "namespace quota",
    "resource requests",
    "limits"
  ],
  "module": "module-5-security",
  "level": "level-13-resourcequota-exceeded"
}
