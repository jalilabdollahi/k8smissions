{
  "name": "Quota Exceeded",
  "description": "A pod has been sitting in Pending for several minutes, but `kubectl get nodes` shows the cluster has available capacity. There is a ResourceQuota on this namespace and the pod is making a resource request that exceeds what the quota allows in total.",
  "objective": "The namespace ResourceQuota is exhausted — reduce resource requests or remove a pod to free up quota",
  "xp": 300,
  "difficulty": "advanced",
  "expected_time": "10m",
  "concepts": [
    "ResourceQuota",
    "LimitRange",
    "requests.cpu",
    "requests.memory",
    "namespace-level governance"
  ],
  "module": "module-5-security",
  "level": "level-3-resourcequota"
}
