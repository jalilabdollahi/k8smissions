The logs should show 'FAILED to bind port 80'. Now inspect the securityContext: `kubectl get pod port-binder -n k8smissions -o yaml | grep -A15 securityContext`. You will see `capabilities.drop: [ALL]` with no corresponding `add` section.