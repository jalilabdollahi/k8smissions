{
  "name": "Capability Gap",
  "description": "A network utility pod needs to listen on port 80, but it exits immediately with 'permission denied' when trying to bind to the socket. The pod correctly runs as a non-root user — but in dropping all Linux capabilities for security, it also dropped the one capability that non-root processes need to bind to ports below 1024.",
  "objective": "The container lacks the NET_BIND_SERVICE capability — add it to the container securityContext",
  "xp": 200,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "securityContext",
    "capabilities",
    "NET_BIND_SERVICE",
    "privileged ports",
    "Linux capabilities"
  ],
  "module": "module-5-security",
  "level": "level-14-capabilities"
}
