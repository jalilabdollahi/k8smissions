{
  "name": "Ghost Deployment",
  "description": "Deployment ghost-deployment exists in the cluster but there are no running pods. Run kubectl get pods -n k8smissions and notice the output is empty.",
  "objective": "The Deployment replica count is set to zero — scale it up so pods are actually running",
  "xp": 150,
  "difficulty": "beginner",
  "expected_time": "8m",
  "concepts": [
    "Deployment",
    "replicas",
    "ReplicaSet",
    "desired vs actual state"
  ],
  "module": "module-2-workloads",
  "level": "level-1-deployment-replicas"
}
