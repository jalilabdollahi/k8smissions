{
  "name": "The Restart Loop",
  "description": "Pods in Deployment api-server keep restarting despite the container starting up cleanly. The RESTARTS column keeps climbing. Run kubectl get pods -n k8smissions.",
  "objective": "The liveness probe path doesn't match any endpoint the app exposes — fix the probe path so it stops killing healthy pods",
  "xp": 200,
  "difficulty": "intermediate",
  "expected_time": "12m",
  "concepts": [
    "livenessProbe",
    "httpGet",
    "tcpSocket",
    "exec",
    "initialDelaySeconds",
    "periodSeconds",
    "failureThreshold"
  ],
  "module": "module-2-workloads",
  "level": "level-3-liveness-probe"
}
