{
  "name": "The Unmovable Update",
  "description": "Deployment api-server had its image updated but the rollout is completely stalled — no new pods have been created and nothing has changed. Run kubectl rollout status deployment/api-server -n k8smissions.",
  "objective": "Both maxSurge and maxUnavailable are set to 0 — fix the RollingUpdate strategy so the rollout can proceed",
  "xp": 200,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "Deployment",
    "RollingUpdate",
    "maxSurge",
    "maxUnavailable",
    "update strategy"
  ],
  "module": "module-2-workloads",
  "level": "level-14-maxsurge-zero"
}
