Fix by setting maxSurge: 1 (allow one extra pod during rollout):
  kubectl patch deployment api-server -n k8smissions \
    -p '{"spec":{"strategy":{"rollingUpdate":{"maxSurge":1,"maxUnavailable":0}}}}'
