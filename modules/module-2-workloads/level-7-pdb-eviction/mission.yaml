{
  "name": "Maintenance is Stuck",
  "description": "A node needs to be drained for maintenance but kubectl drain keeps failing. Run kubectl drain <node-name> --ignore-daemonsets and read the error.",
  "objective": "The PodDisruptionBudget requires more available pods than the Deployment has replicas — reconcile the PDB with the actual replica count",
  "xp": 250,
  "difficulty": "advanced",
  "expected_time": "15m",
  "concepts": [
    "PodDisruptionBudget",
    "voluntary disruption",
    "node drain",
    "minAvailable vs maxUnavailable"
  ],
  "module": "module-2-workloads",
  "level": "level-7-pdb-eviction"
}
