{
  "name": "False Security",
  "description": "Pod database-client is running and connecting successfully. But a security audit has flagged the db-credentials Secret as a potential vulnerability. Run kubectl get secret db-credentials -n k8smissions -o yaml and read what you see.",
  "objective": "The secret is only base64-encoded, not encrypted — demonstrate it is readable and add RBAC to restrict access",
  "xp": 250,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "base64 vs encryption",
    "Secrets at rest",
    "RBAC for secrets",
    "Vault",
    "Sealed Secrets",
    "External Secrets Operator"
  ],
  "module": "module-4-storage",
  "level": "level-7-secret-encoding"
}
