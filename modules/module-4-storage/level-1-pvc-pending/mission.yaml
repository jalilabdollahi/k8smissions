{
  "name": "PVC Won't Bind",
  "description": "Pod database-pod is stuck in ContainerCreating. Run kubectl get pvc -n k8smissions and check the STATUS column of the PVC it depends on.",
  "objective": "The PVC requests more storage than any available PV provides — adjust the PVC size to match what the PV offers",
  "xp": 250,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "PV/PVC binding",
    "capacity matching",
    "StorageClass",
    "kubectl get pv pvc"
  ],
  "module": "module-4-storage",
  "level": "level-1-pvc-pending"
}
