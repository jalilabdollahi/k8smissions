{
  "name": "Wrong Mount Path",
  "description": "Pod web-app crashes on startup even though a volume is mounted. Run kubectl logs web-app -n k8smissions and read the error message.",
  "objective": "The volumeMount path doesn't match the path the application reads config from — correct the mountPath",
  "xp": 200,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "volumeMounts",
    "mountPath",
    "subPath",
    "volume definitions",
    "container filesystem"
  ],
  "module": "module-4-storage",
  "level": "level-2-volume-mount"
}
