apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-data-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain  # ✅ FIX: Retain keeps the volume data after PVC deletion
  storageClassName: standard
  hostPath:
    path: /data/db
