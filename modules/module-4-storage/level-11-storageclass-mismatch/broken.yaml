apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: k8smissions
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: fast-nvme  # ❌ BUG: StorageClass "fast-nvme" does not exist in the cluster
  resources:
    requests:
      storage: 1Gi
