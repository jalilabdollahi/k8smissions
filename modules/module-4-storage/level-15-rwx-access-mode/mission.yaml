{
  "name": "The Exclusive Mount",
  "description": "Multiple pods need to read from and write to a shared PVC simultaneously, but only one pod can mount it at a time. Others are stuck waiting. Run kubectl get pvc shared-pvc -n k8smissions.",
  "objective": "The PVC uses ReadWriteOnce which only allows one node to mount it — change the access mode to ReadWriteMany",
  "xp": 225,
  "difficulty": "advanced",
  "expected_time": "12m",
  "concepts": [
    "PVC",
    "accessModes",
    "ReadWriteOnce",
    "ReadWriteMany",
    "shared storage"
  ],
  "module": "module-4-storage",
  "level": "level-15-rwx-access-mode"
}
