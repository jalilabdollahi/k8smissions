{
  "name": "Missing Config Key",
  "description": "Pod fails to start with a configuration-related error.",
  "objective": "The pod references a ConfigMap key that doesn't match what the ConfigMap actually contains \u2014 fix the key name",
  "xp": 200,
  "difficulty": "beginner",
  "expected_time": "10m",
  "concepts": [
    "ConfigMap keys",
    "configMapKeyRef",
    "envFrom vs env",
    "CreateContainerConfigError"
  ],
  "module": "module-4-storage",
  "level": "level-6-configmap-key"
}