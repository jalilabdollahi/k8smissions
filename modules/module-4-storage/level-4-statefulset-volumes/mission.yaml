{
  "name": "Shared Storage Chaos",
  "description": "A StatefulSet is running but data corruption is occurring between replicas.",
  "objective": "All StatefulSet replicas share a single PVC \u2014 replace it with a volumeClaimTemplate so each pod gets its own PVC",
  "xp": 300,
  "difficulty": "advanced",
  "expected_time": "10m",
  "concepts": [
    "StatefulSet volumeClaimTemplates",
    "per-pod storage",
    "dynamic provisioning",
    "pod-0/pod-1/pod-2 storage isolation"
  ],
  "module": "module-4-storage",
  "level": "level-4-statefulset-volumes"
}