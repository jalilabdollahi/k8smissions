{
  "name": "Permission Denied",
  "description": "Pod writer-app crashes immediately after the volume mounts. Run kubectl logs writer-app -n k8smissions to read the error.",
  "objective": "The volume is owned by root but the container runs as a non-root user — set fsGroup or fix permissions so the container can write",
  "xp": 300,
  "difficulty": "advanced",
  "expected_time": "10m",
  "concepts": [
    "fsGroup",
    "runAsUser",
    "runAsGroup",
    "volume ownership",
    "securityContext at pod vs container level"
  ],
  "module": "module-4-storage",
  "level": "level-8-volume-permissions"
}
