# Common Mistakes — No Workspace

## Mistake 1: Using wrong workspace name

**Wrong approach:** Binding a workspace named 'build' when pipeline expects 'source'

**Correct approach:** Match the exact workspace name from the Pipeline spec
