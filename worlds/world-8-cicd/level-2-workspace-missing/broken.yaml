apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: build-run
  namespace: k8smissions
spec:
  pipelineRef:
    name: build-pipeline
  workspaces: []  # ❌ BUG: Pipeline declares a 'source' workspace but no binding is provided — Tekton fails with 'workspace not bound'
