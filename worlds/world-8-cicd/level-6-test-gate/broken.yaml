apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: run-tests
  namespace: k8smissions
spec:
  steps:
  - name: test
    image: busybox:1.36
    script: |
      #!/bin/sh
      echo "running tests"
      exit 1  # ❌ BUG: hardcoded 'exit 1' always makes the test step fail — pipeline treats non-zero exit as failure and blocks deploy
