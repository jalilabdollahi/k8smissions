{
  "apiVersion": "v1",
  "kind": "Pod",
  "metadata": {
    "name": "dns-heavy-app",
    "namespace": "k8smissions"
  },
  "spec": {
    "dnsConfig": {
      "options": [
        {
          "name": "ndots",
          "value": "2"
        }
      ]
    },
    "containers": [
      {
        "name": "app",
        "image": "busybox:1.36",
        "command": [
          "/bin/sh",
          "-c",
          "while true; do nslookup db.k8smissions.svc.cluster.local; sleep 0.1; done"
        ],
        "resources": {
          "requests": {
            "cpu": "50m",
            "memory": "32Mi"
          },
          "limits": {
            "cpu": "100m",
            "memory": "64Mi"
          }
        }
      }
    ]
  }
}
