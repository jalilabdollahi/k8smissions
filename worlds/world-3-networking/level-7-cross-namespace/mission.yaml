{
  "name": "Cross-Namespace Dead End",
  "description": "A frontend application cannot reach the backend service even though both pods are Running.",
  "objective": "The frontend uses a short service name that doesn't resolve in another namespace \u2014 use the full <service>.<namespace>.svc.cluster.local address",
  "xp": 250,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "Cross-namespace DNS",
    "FQDN format",
    "namespace isolation"
  ],
  "world": "world-3-networking",
  "level": "level-7-cross-namespace"
}