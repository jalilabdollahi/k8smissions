apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: k8smissions
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: standard  # ✅ FIX: use the "standard" StorageClass that exists in the cluster
  resources:
    requests:
      storage: 1Gi
