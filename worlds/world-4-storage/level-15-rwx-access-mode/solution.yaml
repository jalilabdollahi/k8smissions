apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-pvc
  namespace: k8smissions
spec:
  accessModes:
  - ReadWriteMany  # ✅ FIX: RWX allows multiple nodes to mount the volume simultaneously
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi
