{
  "name": "Init Loop",
  "description": "The main application container never starts \u2014 the pod has been stuck in Init state for an hour.",
  "objective": "An init container is failing and retrying \u2014 fix the init container command so it exits 0 and allows the main container to start",
  "xp": 300,
  "difficulty": "intermediate",
  "expected_time": "10m",
  "concepts": [
    "init containers",
    "InitCrashLoopBackOff",
    "pod phases",
    "sequencing"
  ],
  "world": "world-9-scheduling",
  "level": "level-15-init-container-block"
}
