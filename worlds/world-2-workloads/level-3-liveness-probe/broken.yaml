apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-server
  namespace: k8smissions
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api
        image: nginx:alpine
        ports:
        - containerPort: 80
        livenessProbe:
          httpGet:
            path: /nonexistent-healthz  # ❌ Wrong endpoint! nginx returns 404
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 2
